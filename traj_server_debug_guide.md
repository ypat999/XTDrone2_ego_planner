# 轨迹服务器调试指南

## 🎯 概述
本指南说明如何使用RCLCPP_INFO调试轨迹服务器中的方向计算过程。

## 🔧 调试输出说明

### 1. 方向向量调试输出
```
🧭 dir向量: [0.500, 0.866, 0.000], 模长: 1.000
```
- **dir(0)**: FLU坐标系X方向（前向）分量
- **dir(1)**: FLU坐标系Y方向（左向）分量  
- **dir(2)**: FLU坐标系Z方向（上向）分量
- **模长**: 方向向量的长度

### 2. 偏航角计算调试输出
```
📐 atan2(dir(1)=0.866, dir(0)=0.500) = 1.047 rad (60.0°)
```
- **atan2参数**: `atan2(Y分量, X分量)`
- **结果**: 计算的偏航角（弧度）
- **角度**: 转换为度数的结果

### 3. 坐标转换调试输出
```
🔄 坐标转换: FLU(1.000, 2.000, 3.000) -> NED(2.000, 1.000, -3.000)
```
- **FLU**: 原始FLU坐标系位置
- **NED**: 转换后的NED坐标系位置
- **转换规则**: X→Y, Y→X, Z→-Z

### 4. 四元数调试输出
```
📐 四元数: [0.000, 0.000, 0.500, 0.866] (yaw=60.0°)
```
- **四元数**: [x, y, z, w] 格式
- **偏航角**: 对应的偏航角度数

### 5. 最终命令调试输出
```
📤 发布命令: 位置(2.000, 1.000, -3.000), 偏航角=60.0°
```
- **位置**: 最终发布的NED坐标系位置
- **偏航角**: 最终发布的偏航角度数

## 🧪 使用步骤

### 1. 启动轨迹服务器
```bash
# 启动包含轨迹服务器的launch文件
ros2 launch xtd2_launch ros2_single_vehicle_demo_launch.py
```

### 2. 监听调试输出
```bash
# 查看轨迹服务器的日志输出
ros2 topic echo /rosout | grep traj_server

# 或者直接查看控制台输出（如果在前台运行）
```

### 3. 发布测试轨迹
```bash
# 使用测试脚本发布轨迹
python3 test_traj_server_debug.py

# 或者在RViz中设置目标点
```

### 4. 分析输出结果
观察以下关键信息：
- ✅ **方向向量**: 是否合理（长度和方向）
- ✅ **偏航角计算**: 是否与移动方向一致
- ✅ **坐标转换**: 是否正确（特别是Z轴符号）
- ✅ **四元数**: 是否对应正确的旋转

## 📊 常见问题诊断

### 1. 方向向量太短
```
🧭 dir向量: [0.000, 0.000, 0.000], 模长: 0.000
```
**问题**: 轨迹评估失败或轨迹太短
**解决**: 检查轨迹数据是否正确生成

### 2. 偏航角计算异常
```
📐 atan2(dir(1)=0.000, dir(0)=0.000) = 0.000 rad (0.0°)
```
**问题**: 方向向量为零，使用上一次的偏航角
**解决**: 这是正常情况，当无人机悬停时会发生

### 3. 坐标转换错误
```
🔄 坐标转换: FLU(1.000, 2.000, 3.000) -> NED(1.000, 2.000, 3.000)
```
**问题**: Z轴没有取反，坐标转换失败
**解决**: 检查坐标转换代码是否正确

## 🎯 验证技巧

### 1. 直线轨迹测试
设置从(0,0,0)到(5,0,0)的直线轨迹：
- 期望偏航角: 0°（指向东方）
- 期望方向向量: [5, 0, 0]

### 2. 对角线轨迹测试
设置从(0,0,0)到(3,3,0)的对角线轨迹：
- 期望偏航角: 45°（指向东北）
- 期望方向向量: [3, 3, 0]

### 3. 垂直移动测试
设置从(0,0,0)到(0,0,5)的垂直轨迹：
- 期望偏航角: 0°（保持当前方向）
- 期望方向向量: [0, 0, 5]

## 🔍 高级调试

### 添加更多调试点
可以在以下位置添加更多RCLCPP_INFO：
```cpp
// 在轨迹评估后
RCLCPP_INFO(rclcpp::get_logger("traj_server"), "位置评估: (%.3f, %.3f, %.3f)", pos_enu(0), pos_enu(1), pos_enu(2));

// 在速度评估后
RCLCPP_INFO(rclcpp::get_logger("traj_server"), "速度评估: (%.3f, %.3f, %.3f)", vel(0), vel(1), vel(2));

// 在时间计算后
RCLCPP_INFO(rclcpp::get_logger("traj_server"), "时间: t_cur=%.3f, duration=%.3f", t_cur, traj_duration_);
```

### 使用rqt_console
```bash
# 启动rqt_console进行日志分析
ros2 run rqt_console rqt_console
```