# 系统失效降级方案设计（v0.5，2026-09-23）

> 适用范围：Super-LIO + xtd2_communication(comms) + ego_planner/traj_server + uXRCE-DDS + PX4(EV-only) + task_dispatcher（机载 3588）全链。
> 依据：05_46_38、0914 17:57、0918 13:51 三起事件复核结论、`log_345` ULog 实锤，以及 2026-09-23 对
> `multirotor_communication.py` / `uav_service/task_dispatcher` 的代码取证。
>
> **v0.5 定性变更（吸收 gpt-041）**：本版从「故障补丁型安全设计」切换为
> **架构重构**。核心不再问「LIO 挂了怎么办」，而问
> **「现在飞机还剩什么能力、谁拥有控制权」**。目标是把已经实锤的
> 双写者 / 状态分叉 / 共模故障 / 资源争抢这四个真实事故源，用一套
> 显式的 Authority + Capability + Safety State Machine 模型消除，而不是继续往
> H12/H13 堆故障场景。
>
> 明确结论：**不需要推倒 ego_planner / Super-LIO 运动栈**。运动栈（定位 + 局部轨迹规划）
> 作为「Motion Executor」保留；要重构的是**控制面 / 安全面 / 任务面**的权责与故障域划分。
>
> v0.4.1 已识别的三个存量缺陷（offboard 双写者 / 航段无超时 / 返航-disarm 双链）
> 与资源争抢，本次均按代码重新取证确认，并作为 P-ARCH 的前置理由。

---

## 引. 当前架构速览（评审导读，2026-09-23 代码基线）

> 本节描述**现有代码的实际架构**（即 v0.4.1 所依据的现状），供评审先建立基线；
> §0 起为 v0.5 目标架构。每条现状问题都标注了本文给出解法的节号。
>
> 导读顺序：**引.1 数据流总图 → 引.2 模块清单（按功能域）+ 引.2.1 FMU 写入方全量取证
> → 引.3 进程/资源 → 引.4 comms 职责 → 引.5 已落地/未落地 → 引.6 问题锚点表**。

### 引.1 一张图看懂当前数据流（真机链路）

```text
云端 MQTT ◀────────► task_dispatcher（机载 3588，systemd 域，未绑核）
                       │ 两条路径并存：
                       │ ①巡航：perform_action_walk_to_pos_3d
                       │   while True @20Hz 刷 /goal_pose_3d（无超时）
                       │ ②手动：/uav_command → UavMissionManager（活）
                       │   → 直写 /fmu/in/*（3 活 + 2 潜伏写者，见引.2.1）
                       ▼
/goal_pose_3d ┌───────┴────────┐        ← goal 双入口（现状！）
   ┌──────────┴───────┐  ┌─────┴───────────┐
   │ comms (5,6)      │  │ xtd2_traj_server │ ← 未绑核，漂浮
   │ goal_marker_cb   │  │ (直接收新goal；  │
   │ → auto-switch:   │  │  safe_zone下降)  │
   │   arm→takeoff→   │  │ 时间推进bspline   │
   │   offboard       │  │ 20Hz 发 ────────────────┐
   │ 就绪后转发        │  │ 收 /stop_planning        │
   │ /move_base_simple│  └──────────────────┘       │
   │ /goal ─────────┐ │                             │
   └──────┬─────────┘ │   ego_planner_node (5,6)    │
          │           └──▶ FSM重规划→planning/bspline│
          │                    ▲                    │
          │                    │ odom_world=/lio/odom│
          │                    │ grid_map/cloud=     │
          │                    │  /lio/cloud_world + │
          │                    │  pose=/mid360/pose  │
          ▼                    │                     ▼
 /fmu/in/trajectory_setpoint + offboard_control_mode(20Hz心跳，
   （FLU→NED 经 TF world→px4_odom，comms 重发最后 setpoint）◀─┘
          ▲
Super-LIO /lio/robo/odom(~200Hz，绑核 4/7) ──▶ comms 逐帧守卫
   （stale/gap/跳变/协方差 sanitize/reset_counter；限频 20Hz）
      ──▶ /fmu/in/vehicle_visual_odometry（PX4 EKF2 唯一绝对定位源）
          ▲
          │  /fmu/out/{vehicle_status, vehicle_local_position,
          │   vehicle_odometry, vehicle_land_detected, vehicle_command_ack}
          ▼
 uXRCE-DDS Agent（绑核 0-3，udp4:8888）◀──▶ PX4 FMU
 comms 消费出向：failsafe 检测 / 跳变急刹 / 降落监护+1Hz强制disarm /
                温度保护(>86°C 降落) / nav_state 状态机维护
旁路：lidar_localization(/pcl_pose，未绑核)、obstacle_distance_publisher
     (点云→PX4 碰撞回避，0-3)、goal_pose_marker(RViz 手动打点，也发 /goal_pose_3d)
     媒体域：RTSP ffmpeg 录制/预览、pcd 分片上传、algo 推理（均未绑核）
```

### 引.2 模块清单（按功能域，2026-09-23 代码基线）

> 命名约定：`<ns>` 为命名空间（单机为空串），`xtdrone2_topic_prefix = /xtdrone2/<ns>/`，
> `dds_topic_prefix = /<ns>/`（见 [multirotor_communication.py](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_communication/xtd2_communication/multirotor_communication.py#L221-L242)）。

#### A. 定位与感知域

| 模块 | 载体 / 文件 | 职责 | 关键接口（入 → 出） | 绑核 | 现状问题 |
|---|---|---|---|---|---|
| **Super-LIO** | [lio/Super-LIO](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/lio/Super-LIO) | LIO 里程计 + 世界点云，**PX4 EV 的唯一绝对定位源** | `/livox/lidar` → `/lio/robo/odom`(~200Hz)、`/lio/body/cloud`、`/lio/cloud_world`、`/lio/odom_reset_counter` | 主 4 / 子 7 | 无对外健康上报；断流 ~4.5s 后 PX4 判 `local_position_invalid`（悬崖） |
| **livox_ros_driver2** | [lio/livox_ros_driver2](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/lio/livox_ros_driver2) | mid360 雷达驱动 | → `/livox/lidar` | — | 真机 `xfer_format=1` 发 CustomMsg，CP 节点无法直订（改订 `lio/body/cloud`） |
| **lidar_localization_ros2** | [lio/lidar_localization_ros2](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/lio/lidar_localization_ros2) | 先验地图全局定位（地图系锚定） | `/lio/cloud_world` + 先验图 → `/pcl_pose`（TRANSIENT_LOCAL） | **未绑** | 是 arm 闸门（`--require-pcl-pose`）与调度侧状态源；无健康上报；漂浮 |
| **obstacle_distance_publisher** | [obstacle_distance_publisher.py](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_communication/xtd2_communication/obstacle_distance_publisher.py) | 点云 → PX4 碰撞回避测距 | `lio/body/cloud` → `<ns>/fmu/in/obstacle_distance` | 0-3 | **合法 FMU 写者**（CP 专线，不参与控制权竞争） |

#### B. 控制与通信域

| 模块 | 载体 / 文件 | 职责 | 关键接口（入 → 出） | 绑核 | 现状问题 |
|---|---|---|---|---|---|
| **multirotor_communication (comms)** | [multirotor_communication.py](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_communication/xtd2_communication/multirotor_communication.py)（2089 行） | **目标上的唯一 FMU 写者**；目前还兼任任务/安全逻辑 | 入：`cmd_pose_local_ned`/`cmd_trajectory_flu`/`cmd_vel_*`/`cmd_accel_*`/`cmd_attitude_flu`、`XTD2Cmd` 服务、PX4 out×6、`/lio/robo/odom`、`/lio/odom_reset_counter`、`/pcl_pose`、`/goal_pose_3d` → 出：`/fmu/in/{vehicle_command,offboard_control_mode,trajectory_setpoint,vehicle_attitude_setpoint}`、`/fmu/in/vehicle_visual_odometry`、`/move_base_simple/goal`、`/stop_planning`、`debug/vehicle_state` | 5,6 | 单点超载；与 planner 同池互抢；**无 battery 订阅 / 无 `/mission_status`** |
| **tf_publisher** | [xtd2_launch](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_launch) | 发布 `world↔px4_odom` 等 TF（comms 做 FLU→NED 依赖它） | → `/tf` | 0-3 | 缺 TF 时 setpoint 转换失败 |
| **MicroXRCEAgent** | 系统服务 | ROS2 ↔ PX4 uORB 的 DDS 桥 | `udp4:8888` | 0-3 | 单点；systemd 自动重启策略待核（B2） |

#### C. 规划域

| 模块 | 载体 / 文件 | 职责 | 关键接口（入 → 出） | 绑核 | 现状问题 |
|---|---|---|---|---|---|
| **ego_planner_node** | [ego-planner-swarm](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_third_party_pkgs/motion_planning/ego-planner-swarm/src/planner/plan_manage) | 重规划 FSM + grid_map + B 样条优化 | 入：`odom_world`、`grid_map/{cloud,odom,pose}`、`/move_base_simple/goal`、`/stop_planning` → 出：`planning/bspline` | 5,6 | 与 comms 同池；无「无进展/求解失败」上报；`fsm/fail_safe` 开启但无外部仲裁 |
| **xtd2_traj_server** | [xtd2_traj_server.cpp](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_third_party_pkgs/motion_planning/ego-planner-swarm/src/planner/plan_manage/src/xtd2_traj_server.cpp) | B 样条时间推进 → 位置指令；含 safe-zone 内下降 | 入：`planning/bspline`、`odom_world`、**`/goal_pose_3d`（直订）**、`/stop_planning` → 出：`/xtdrone2/<ns>/cmd_trajectory_flu` | **未绑** | **直订 `/goal_pose_3d` = goal 双入口之一**（绕过 comms 闸门）；无航段超时；漂浮 |
| **goal_pose_marker** | xtd2_launch | RViz 手动打点，发 `/goal_pose_3d` | → `/goal_pose_3d` | — | 人工旁路入口，不受任何闸门约束 |

#### D. 任务编排域（task_dispatcher，机载 3588）

| 模块 | 文件 | 职责 | 现状问题 |
|---|---|---|---|
| **task_dispatcher_node** | [task_dispatcher_node.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/task_dispatcher_node.py) | MQTT 主入口 + 17 类 `cmd` 分发字典 + MQTT 重连/心跳 | 调度器本身无租约/无失联保护（H2） |
| **remote_control** | [remote_control.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/remote_control.py) | 执行 `takeoff/land/walk/face/charge/dock/ptz/...` | **实例化 `UavMissionManager`（FMU 写者）+ `UavCommandSender`**；`speed_map` 为四足遗留 |
| **cruise_uav_task_manager** | [cruise_uav_task_manager.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/cruise_uav_task_manager.py) | 巡检任务编排（`SETCRUISETASK` 等） | **实例化 `UavActionManager`（FMU 写者）**；自带 `speed_map` |
| **perform_action_manager** | [perform_action_manager.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/perform_action_manager.py) | 动作执行；`perform_action_walk_to_pos_3d` 为巡航主路径 | **`while True`@20Hz 刷 `/goal_pose_3d` + 自测距离判到达，无超时**（0918「悬到没电」机制） |
| **uav_mission_manager** | [uav_mission_manager.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_manager.py) | 机载 offboard 状态机副本（ARMING→TAKEOFF→HOVERING→LANDING→DISARMING） | **直写 3 个 `/fmu/in/*` + 发 `/goal_pose_3d`**；由 remote_control 无条件实例化 → 活的双写者 |
| **uav_action_manager** | [uav_action_manager.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_action_manager.py) | 动作级指令封装 | **直写 `/fmu/in/vehicle_command`**；由 cruise_uav_task_manager 实例化 → 活的双写者 |
| uav_mission_core | [uav_mission_core.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_core.py) | 同上（又一份状态机副本） | 含 `/fmu/in/*` publisher，但**当前无调用方**（潜在第六写者） |
| **ros2_topic_subscriber** | [ros2_topic_subscriber.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/ros2_topic_subscriber.py) | 状态采集与上报：`/rkbot/*`、`/amcl_pose`、`/pcl_pose`、`/algo_result/*`，以及 **PX4 out**（`vehicle_status`、**`battery_status`**、`failsafe_flags`、`local/global_position`、`attitude`） | **调度侧已能拿到 battery/failsafe 数据**（comms 没有）；但无统一出口契约（§13） |
| registration / status_report / alarm_reporter | [registration.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/registration.py) 等 | 注册保活（KeepAlive/ACK）、状态上报、告警上报 | — |
| map_manager / point_cloud_* / cloud_compare_icp / pcd_chunk_upload | [map_manager.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/map_manager.py) 等 | 建图、点云裁剪/压缩/持久化/分片上传、ICP 比对 | 飞行期 CPU/IO 争抢来源（§12/H6） |
| rtsp_record_manager(_ffmpeg) / video_preview_manager / ptz_control | — | 录像/预览/PTZ | 飞行期媒体开销（§12） |
| initial_pose_manager / preflight_check_node | [initial_pose_manager.py](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/initial_pose_manager.py) 等 | `INITIALPOSE` 自动注入、起飞前故障检查 | preflight 当前被注释掉（`#self.create_timer(300, ...)`）→ READY gate 缺位（H1） |
| dock_control_client / device_control / command_deliver / service_manager | — | 机库控制、设备控制、远程指令、服务管理 | 机库联锁与充电编排入口 |

#### E. 交互 / 调试域（xtd2_control）

| 模块 | 文件 | 职责 | 现状问题 |
|---|---|---|---|
| **uav_mission_server** | [uav_mission_server.py](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_control/xtd2_control/keyboard/uav_mission_server.py) | 订阅 `/uav_command` → 自建 ARMING/TAKEOFF/HOVERING/LANDING 状态机 → **直写 3 个 `/fmu/in/*`** | **与 `UavMissionManager` 逐行同构的重复实现**；`setup.py` **未注册入口**（当前不会被自动拉起，属潜伏写者） |
| multirotor_keyboard_control | [multirotor_keyboard_control.py](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_control/xtd2_control/keyboard/multirotor_keyboard_control.py) | 键盘遥控调试 | 人工旁路 |

#### F. 消息与接口域

| 接口 | 文件 | 用途 | 现状 |
|---|---|---|---|
| `XTD2Cmd.srv` | [XTD2Cmd.srv](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_msgs/srv/XTD2Cmd.srv) | `string command → bool success` | comms 已提供 `/xtdrone2/<ns>/cmd` 服务，但**当前无任何调用方**（规范命令通道空置）→ 正是 P-ARCH 应收敛到的通道 |
| `XTD2VehicleState.msg` | [XTD2VehicleState.msg](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_msgs/msg/XTD2VehicleState.msg) | 飞控状态快照（offboard_state/位置/初始/ts） | comms 10Hz 发 `debug/vehicle_state`，调度侧未订 → 可作 `/mission_status` 的现成素材 |

#### G. 仿真与外部依赖

| 模块 | 说明 |
|---|---|
| xtd2_gz_sim / gz_launch / xtd2_vehicle_spawn_launch / uav_simulator | Gazebo world、模型生成、PX4 SITL 仿真链（真机分支不启动） |
| xtd2_third_party_pkgs/px4_msgs | PX4 uORB 消息定义 |
| PX4-Autopilot（外部） | EV-only 定位、failsafe 阶梯（§11 审计对象） |

#### 引.2.1 FMU 写入方清单（`/fmu/in/*` 全量取证）

| # | 写入方 | 进程域 | 写入话题 | 状态 |
|---|---|---|---|---|
| 1 | `multirotor_communication` | 机载机控域 | `vehicle_command`、`offboard_control_mode`、`trajectory_setpoint`、`vehicle_attitude_setpoint`、`vehicle_visual_odometry` | ✅ 应为**唯一**控制写者 |
| 2 | `UavMissionManager` | task_dispatcher | `offboard_control_mode`、`trajectory_setpoint`、`vehicle_command`（另发 `/goal_pose_3d`） | ❌ **活的**第二写者（经 `RemoteControl` 无条件实例化） |
| 3 | `UavActionManager` | task_dispatcher | `vehicle_command` | ❌ **活的**第三写者（经 `CruiseUavTaskManager` 实例化） |
| 4 | `uav_mission_server` | xtd2_control | `offboard_control_mode`、`trajectory_setpoint`、`vehicle_command` | ⚠️ 潜伏（未注册入口；一旦手启即成第四写者） |
| 5 | `UavMissionCore` | task_dispatcher | `offboard_control_mode`、`trajectory_setpoint`、`vehicle_command` | ⚠️ 潜伏（当前无调用方） |
| 6 | `obstacle_distance_publisher` | 机载机控域 | `obstacle_distance`（CP 专线） | ✅ 合法，不参与控制权竞争 |

**结论**：`/fmu/in/*` 上同时存在 **3 个活写者 + 2 个潜伏写者**，且其中至少两个（#2/#4）带有各自独立的 arm/takeoff/offboard 状态机。
这坐实了 gpt-041 的核心判断：**问题不是「故障太多」，而是控制权本身没有单一化**。P-ARCH（§15）必须先做。

同时可见：`/uav_command` 这条链上，**发布方有 3 处（remote_control / perform_action_manager / cruise_uav_task_manager），订阅方是 #2 与 #4**——
即「云端点 takeoff」实际由 `UavMissionManager` 直接写 FMU，而非经 planner 或 comms 闸门。这也是 §14-1 的代码证据。

### 引.3 进程与资源现状（RK3588：4×A76 + 4×A55，核号需 `lscpu -e` 校准）

| 进程 | 绑核（launch 实测） | 角色 | 现状问题 |
|---|---|---|---|
| multirotor_communication | **5,6** | FMU I/O + 大量任务/安全逻辑 | 与 planner 同池互抢；单点超载 |
| ego_planner_node | **5,6** | replan FSM + B-spline | 同上 |
| xtd2_traj_server | **未绑** | 轨迹时间推进→setpoint | 漂浮进任意核 |
| super_lio 主/子 | 4 / 7 | LIO 定位（~200Hz odom） | 合理 |
| MicroXRCEAgent | 0-3 | ROS↔PX4 DDS 桥 | 单点，systemd 自动重启待核 |
| lidar_localization | 未绑 | 全局 map 系定位 /pcl_pose | 漂浮 |
| task_dispatcher 域 | 未绑 | MQTT↔ROS、巡航编排、媒体 | 含直写 `/fmu/in/*` 的旁路 |

### 引.4 comms 当前职责清单（即 §5 要瘦身进 supervisor / Mission Manager 的东西）

FMU I/O 与心跳/EV/setpoint 发布（保留项）之外，还承担：
auto arm→takeoff→offboard 状态机（`auto_switch_to_egoplanner`，30s 超时只重置闩锁）、
goal 缓存/转发/拦截（manual_offboard_exit）、PX4 failsafe 检测与急刹车、
降落监护四阶段（触地→stop_planning→position→LAND→1Hz 强制 disarm）、
板温保护、EV 质量守卫（stale/gap/跳变/sanitize——保留项）。
**没有**：battery 订阅、goal_seq、`/mission_status`、safety_hold、V-B。

### 引.5 已落地 vs 未落地基线（避免评审误把提案当现状）

- ✅ **已落地**：EV 限频 20Hz + BE 订阅、stale/gap/跳变守卫、协方差 sanitize +
  reset_counter 单调合成、心跳前置 try/except、auto-switch、降落监护+强制 disarm、
  温度保护、`/stop_planning`、障碍测距（PX4 CP）、`require_pcl_pose` arm 闸门。
- ❌ **未落地（v0.2~v0.4.1 全部为提案）**：xtd2_safety、`/mission_status`、
  `/mission_event`、goal_seq/flight_epoch、`/goal_vel_max`、battery 监测、
  V-B 盲降、breadcrumbs 返航、safety_hold、READY gate、错误预算。

### 引.6 现状问题锚点（问题 → 本文解法）

| # | 现状问题（代码证据见引.2.1 / §14 / 附录） | 解法所在 |
|---|---|---|
| 1 | **FMU 多写者**：`/fmu/in/*` 上 3 个活写者（comms + `UavMissionManager` + `UavActionManager`）+ 2 个潜伏写者，其中两个自带独立 arm/takeoff 状态机 | §1 Authority、§14-1、P-ARCH |
| 2 | **goal 双入口**：comms 经闸门转发 `/move_base_simple/goal`，而 traj_server 直订 `/goal_pose_3d`（绕过闸门）；无 seq/epoch | §4、§13、P7 |
| 3 | **飞段无超时**：`while True`@20Hz 刷 goal + 调度侧自测距离判到达 | §14-2、§9.1、P7/P9 |
| 4 | **返航/disarm 双链**：`handle_disarming_state` 循环 land+disarm 与机载监护互抢 | §14-3、§9.2 |
| 5 | **无机载统一状态出口**：外部只能"盯位置猜到达"，comms 死=失声 | §13、H8 |
| 6 | **无 readiness 闸门**：开机靠 `TimerAction` 延时 + `wait_for_px4_odom` 话题轮询兜底；`preflight` 定时器被注释 | §17-H1、P10 |
| 7 | **资源共池与漂浮**：控制面与规划共池 5,6；traj_server / lidar_localization / 媒体 / 上传均未绑核 | §12、P1 |
| 8 | **comms 单点超载**：2089 行内混装 FMU I/O + 任务状态机 + 安全决策 | §1/§5（comms 瘦身为四件事） |
| 9 | **规范命令通道空置**：`XTD2Cmd` 服务无调用方，实际命令走 `/uav_command` 私有约定（`string` 无 schema、无 ack/seq） | §1、P-ARCH |
| 10 | **状态机四份副本**：`UavMissionManager` / `UavMissionCore` / `uav_mission_server` / comms auto-switch 各自维护 arm/takeoff/land 逻辑 | §1、§14（退役为 Mission Manager 单份） |

---

## 0. 第一原则（取代原「降级取决于存活集」的表述）

> **系统不应围绕「故障处理函数」设计，而应围绕「当前仍然可信的能力集合 + 当前拥有控制权的 Authority」设计。**

推论：

1. 面对**未知故障**，正确的问题不是「这个故障对应哪个 handler」，而是
   **「它让哪些 capability 从 1 变成 0，以及哪个 Authority 因此失效」**。
2. 安全决策只依赖**能力向量**，不依赖**故障种类**。新增故障 = 新增一条
   「capability 归零」映射，而不是新增一个降级流程。
3. **Authority 单一化**：飞行意图、运动执行、安全仲裁三个权力分离，且
   **PX4 只能有一个写者（comms）**。

## 1. 三大 Authority（控制权模型）

| Authority | 载体 | 回答的问题 | 拥有 | 禁止 |
|---|---|---|---|---|
| **Mission Authority**（任务权） | `task_dispatcher` | 「我要去哪、按什么顺序、多快」 | GOTO / SKIP / RETRY / RETURN(任务级) / DO_ACTION / DOCK | **禁止直接碰 PX4**（不得发 `/fmu/in/*`） |
| **Motion Authority**（运动权） | `ego_planner` + `traj_server`（经 comms 落盘） | 「飞机现在怎么飞」 | 产生 trajectory intent | 禁止绕过 comms 直接写 FMU |
| **Safety Authority**（安全权） | `xtd2_safety` | 「现在还能不能这么飞」 | 健康门/仲裁/override/降级 | 禁止决定「下一站去哪」 |

**FMU 唯一写者 = comms**。这是全系统最高优先级，先于一切授权机制：

```text
task_dispatcher ──MissionIntent──▶ Mission Manager（状态机）
                                        │ motion intent
                                        ▼
                                  ego_planner/traj_server
                                        │ MotionIntent
                                        ▼
                                  xtd2_safety（安全门/仲裁）
                                   ┌────┴────┐
                            MISSION TRAJ    SAFETY OVERRIDE
                                   └────┬────┘
                                        ▼
                                      comms（唯一 FMU 写者）
                                        ▼
                                       PX4
```

Mission Authority 永远不能覆盖 Safety Authority 的 RETURN/LAND：
RETURN 产生的是 **Safety trajectory**，不是 **Mission goal**，因此
`task_dispatcher` 无法把已经进入 RETURN 的飞机重新拉回 MISSION 状态机（见 §3）。

## 2. 能力模型（Capability Vector）

取代「LIO 挂 → 某降级函数」的硬编码。仲裁器只看能力向量：

```text
POSITION_VALID          位置可信（LIO 新鲜 + 协方差界内）
VELOCITY_VALID          速度可信
EV_OUTPUT_VALID         发往 PX4 的 EV 通道可信
PX4_LINK_VALID          fmu/out 入向链路可达
OFFBOARD_ALIVE          offboard 会话存活
CONTROL_LOOP_ALIVE      comms 控制回路（心跳/EV 发布）存活
PLANNER_ALIVE           规划器/轨迹输出存活
TASK_LAYER_ALIVE        任务层（dispatcher）租约存活
BATTERY_VALID           电量可信且高于保底线
GROUND_CONTACT_VALID    触地判定可信
LOCAL_MAP_VALID         局部栅格/ESDF 有效
RETURN_PATH_VALID       存在可回放的返航轨迹
```

降级决策表（能力组合 → 动作，全序只升不降）：

```text
POSITION=1  CONTROL=1  PLANNER=1  BATTERY=OK          → MISSION
POSITION=1  CONTROL=1  PLANNER=0                     → HOVER → LAND(V-A)
POSITION=0  CONTROL=1                                → BLIND LAND(V-B)
CONTROL=0                                           → PX4 FAILSAFE(L5 Descend)
GROUND_CONTACT=1                                    → DISARM
TASK_LAYER=0                                        → MISSION FREEZE → HOVER → RETURN/LAND
```

**每个失效场景不再写「怎么办」，只写「让哪些 capability 归零」**（§8 全表按此重列）。

## 3. 安全状态机（xtd2_safety）

`xtd2_safety` 是整个系统的「飞行安全 PLC」，分三轴：

```text
        ┌──────────────┐
        │  SAFETY STATE │
        └──────┬───────┘
    ┌──────────┼──────────┐
    ▼          ▼          ▼
  HEALTH    AUTHORITY   ACTION
```

状态（单向只升危险等级）：

```text
INIT → READY → MISSION → DEGRADED → RETURN → LAND → LANDED
                            │
                            └──────────▶ HOLD（可回到 MISSION，见下）
```

**单向闩锁原则**：`MISSION → DEGRADED → RETURN → LAND → LANDED` 只升不降。
只有两类「下降」被允许：

1. `HOLD` 是任务级暂停，不是危险升级，允许在 clearance 恢复后回到 MISSION；
2. `LAND → MISSION` 只允许经由 `RESET`（明确的生命周期边界：disarm + 新 flight epoch，§4）。

这把原方案「`CRITICAL_LAND > BLIND_LAND > RETURN > HOVER > MISSION` 优先级只升不降」
从一个安全策略升级为**整个系统的状态机基础规则**，并直接解决 H5（跳过→返航→再起飞环路）。

## 4. Flight Epoch / Generation 模型

在 `goal_seq`、`generation` 之上新增 `flight_epoch`：

```text
flight_epoch = 20260923-001        # 每次 ARM / 新飞行任务启动时 +1
```

所有 `goal / trajectory / mission_event / safety_event / return_path / land / disarm`
全部绑定 `(flight_epoch, goal_seq, trajectory_seq)`。目的：**拒绝幽灵状态**。

```text
Flight 12 goal 38 trajectory 91 → 落地 → Flight 13 goal 1
迟到的 trajectory 91 / goal 38 / RETURN   → REJECT（不是「碰巧又合法」）
```

`goal_seq` 是 generation 的第一现场（旧 goal 的失败事件不打到新 goal 上）；
`flight_epoch` 是 generation 的**终局边界**（跨架次的任何迟到状态一律拒绝）。

## 5. 组件拓扑（6 + 2 组件）

```text
┌────────────────────────────┐
│        CLOUD / MQTT         │   任务发起/暂停/撤销、遥测、KeepAlive
└──────────────┬─────────────┘
               │ MissionIntent(flight_epoch, goal_seq, pose, vel_limit, arrive_radius, timeout)
               ▼
┌────────────────────────────┐
│     TASK DISPATCHER         │   航点编排/retry/skip/dock/事件桥接上云
│   （Mission Authority）      │   禁写 /fmu/in/*；禁自行判飞机状态
└──────────────┬─────────────┘
               │ motion intent
               ▼
┌────────────────────────────┐
│    MOTION EXECUTOR          │   ego_planner / traj_server 轨迹生成
│   （Motion Authority）       │
└──────────────┬─────────────┘
               │ MotionIntent
               ▼
┌────────────────────────────┐
│       SAFETY GATE           │   xtd2_safety：健康门/能力向量/状态机/仲裁/override
│   （Safety Authority）       │
└──────┬──────────────┬──────┘
 MISSION TRAJ      SAFETY OVERRIDE
       └──────┬─────┘
              ▼
┌────────────────────────────┐
│           COMMS             │   唯一 FMU 写者：EV/心跳/setpoint/指令执行
│                             │   只判「合法+不过期」，不判「该不该」
└──────────────┬─────────────┘
               ▼
             PX4（最终独立 failsafe）

旁路： Super-LIO（localization + health evidence）
       System Monitor（CPU/RAM/IO/disk/thermal）
```

## 6. 三条硬前提（决定「哪些降级存在」，物理事实不变）

1. **EV 是唯一绝对定位源 → 悬崖效应**。EV 断流约 4~5s 后 PX4 判
   `local_position_invalid`（0918 ULog 实测），此后 Hold/Position/AUTO_LAND/RTL
   全部不可运行，只剩 Descend（无水平控制、随风漂）。
   → 一切需要位置的降级动作必须在断流后 ~3s 内决策。
2. **PX4 侧 RTL/AUTO_LAND 够不着**。无全局定位 → `home_position_invalid`。
   「返航」只能是规划器侧轨迹返航（Safety trajectory）；「降落」分两腿：
   位置有效走 AUTO_LAND（V-A），位置失效自建盲降（V-B）。
3. **comms 是控制链单点**。它死了没有外部手段能救，兜底只剩 PX4 参数阶梯。
   → comms 自身防护 + §11 参数表本身是降级体系的一部分。这也是「comms 只做
   FMU I/O + 完整性保护，不做决策」的根源：单点上的决策越多，共模风险越大。

## 7. 降级阶梯（动作定义，按能力依赖标注）

| 层级 | 动作 | 执行者 | 能力依赖 | 现状 |
|---|---|---|---|---|
| L0 | 带伤续飞（扣帧/跳变守卫/BE 丢过期帧） | comms 守卫 | — | ✅ 已有 |
| L1 | 悬停保持（零速 setpoint 冻结） | comms | POSITION_VALID | ✅ 已有（brake/hover） |
| L2' | goal 停滞上报（报告不决策） | comms → 外部 | — | 🆕 §9.1 |
| L3' | 安全返航（Safety trajectory，非 mission goal） | Motion Executor + comms | EV+规划器+位置可信+RETURN_PATH_VALID | 🆕 §9.2 |
| L4a | 原地降落 V-A（AUTO_LAND） | PX4 + comms 监护 | POSITION_VALID | ✅ `trigger_landing` |
| L4b | 原地盲降 V-B（速度下降 + 触地检测） | comms 直发 setpoint | 仅 PX4_LINK_VALID | 🆕 **全场最值钱** |
| L5 | PX4 failsafe Descend | FMU 自动 | — | ✅（漂移是模式天性） |
| L6 | 触地强制 disarm 1Hz 重试 | comms 降落监护 | GROUND_CONTACT_VALID | ✅ 已有 |

## 8. 失效场景 × 检测 × 能力归零（重列）

> 每行只新增「能力影响」列：该场景让哪些 capability 归零，仲裁器据此动作，
> 不再为每个故障单独设计降级流程。

### A. 感知 / LIO 链

| # | 场景 | 检测 | 能力归零 | 路由（由能力组合推导） |
|---|---|---|---|---|
| A1 | LiDAR 退化（玻璃/粉尘/特征缺失） | 🆕 协方差贴 `EV_COV_*MAX` 占比、`_odom_reset_out` 速率 | LOCAL_MAP_VALID↓ | YELLOW→禁返航、上报；持续→L3'或L4b |
| A2 | LiDAR 断连/无点云 | ✅ odom `gap>0.3s`；🆕 gap>1s→RED | POSITION_VALID=0 | RED→**L4b（3s 内定夺）** |
| A3 | LIO 发散/跳变 | ✅ `_odom_jump_cnt`；🆕 速率化 N 次/30s | POSITION_VALID=0 | 频发→RED→L4b |
| A4 | LIO 进程死 | ✅ odom 全停 | POSITION_VALID=0 | 同 A2 |
| A5 | 陈旧帧/时标倒退 | ✅ stale 计数 | EV_OUTPUT_VALID=0 | 成片→按 A2 |
| A6 | z 基准污染（baro/测距） | 🆕 PX4 lpos.z 与 LIO z 交叉核对 | POSITION_VALID↓ | L1→上报；兜底 L4b（降速压动能） |

### B. 通信链

| # | 场景 | 检测 | 能力归零 | 路由 |
|---|---|---|---|---|
| B1 | uXRCE 入向断 | 🆕 往返探针 0.5Hz（幂等 `VEHICLE_CMD_USER_1`→`vehicle_command_ack`） | PX4_LINK_VALID=0 | 广播 `LINK_OUTBOUND_DEAD`；真兜底=§11 `COM_OF_LOSS_T=2`+`COM_OBL_RC_ACT` |
| B2 | agent 崩 | 同 B1 | PX4_LINK_VALID=0 | 同 B1 |
| B3 | 心跳降频（executor 饥饿） | 🆕 自检 timer 实频<10Hz | CONTROL_LOOP_ALIVE↓ | 限速缓解；持续上报，地面改派 L4a |
| B4 | 出向断（comms「失明」） | 🆕 vehicle_status/odometry 到达 watchdog（>2s 无） | 决策可信度=0 | **立即冻结决策、只维持 L1+心跳、广播 `LINK_BLIND`** |

### C. 规划 / 任务层

| # | 场景 | 检测 | 能力归零 | 路由 |
|---|---|---|---|---|
| C1 | traj_server/planner 死 | ✅ `cmd_trajectory_flu` 断流 | PLANNER_ALIVE=0 | POSITION=1→L1 悬停 30s→L4a；**绝不自动 V-B** |
| C2 | goal 不可达/无进展 | 🆕 goal 进展 watchdog（期望速度=该段限速） | TASK_LAYER_ALIVE 不变（上报） | L2' `GOAL_STALLED`；超时→L3'→L4 |
| C3 | 轨迹求解反复失败 | 🆕 traj_server 失败/时间预算耗尽计数 | PLANNER_ALIVE↓ | 同 C2 |
| C4 | 坏 goal（越界/地下/超远） | 🆕 comms 入链前合法性检查 | — | 拒绝+`GOAL_REJECTED`，不触发 auto-switch |
| C5 | 起飞序列卡住 | ✅ ack callback 现成，🆕 接超时判定 | TAKEOFF 状态 | 放弃起飞→disarm→IDLE→上报 |

### D. 电源 / 环境 / FMU

| # | 场景 | 检测 | 能力归零 | 路由 |
|---|---|---|---|---|
| D1 | 电量黄 | 🆕 comms **目前无 battery 订阅**（第一块缺口） | BATTERY_VALID↓ | 上报；`T_ext` 无干预→本地 L3' |
| D2 | 电量红/电压急坠 | 🆕 阈值+下降速率 | BATTERY_VALID=0 | 不等外部：L4a（位置有效）/L4b（临界） |
| D3 | 板卡过热 | ✅ >86°C 已有降落 | — | 已有 |
| D4 | FMU 空中重启 | **软件无解** | CONTROL=0 | 硬件缓解（独立供电/电容），如实标注 |
| D5 | IMU/执行器失效 | FMU failure_detector 自理 | — | 不介入 |

### G. 人工阀

| # | 场景 | 问题 | 建议 |
|---|---|---|---|
| G1 | 无人工接管通道 | `COM_RC_IN_MODE=3` 禁摇杆 | RC 三段开关→DDS 转发 HOVER/LAND |

## 9. 三大动作具体设计（映射到 Safety trajectory 语义）

### 9.1 goal 停滞 → 可达性微修复（备用点）→ 上报（L2'）→ 安全接管
- 权限切分：任务顺序取舍归外部；「怎样到达这个点」归内层（备用点微修复）。
- 三级链路：
  1. **停滞检测**：期望速度取该段 `/goal_vel_max`（未发用默认），
     `T_goal = 路径长/期望速度 × 系数(2.5)`。`dist < 到达半径` 时只报 `ARRIVED_UNACK`，永不自行离场。
  2. **可达性微修复（traj_server 侧）**：极网格搜索（r∈{0.3,0.6,1.0,1.5}m × 8 方位，≤32 格）
     → 失败则 Climb-to-Clear 重评 → 仍失败进第 3 级。
  3. **上报 + 宽限 + 安全链**：`GOAL_UNREACHABLE` → L1 悬停 → `T_ext=30s` → L3' → L4。

### 9.2 安全返航（L3'）：错误预算触发 + 重放回程（Safety trajectory）
- **触发 = 错误预算**（单机累计、只增不减，随 flight 复位）：

  | 事件 | 计权 | 单项上限 |
  |---|---|---|
  | `GOAL_UNREACHABLE`（含微修复失败） | 3 | 2 |
  | goal 停滞超 `T_ext` 无干预 | 2 | 2 |
  | LIO YELLOW episode | 2 | 3 |
  | 链路抖动（B1/B3/B4） | 2 | 3 |
  | planner/traj_server 异常重启 | 3 | 1 |
  | 电量黄 | — | 直接触发 |

  总分 ≥6 同样 `RETURN_INIT`。
- **航路 = 重放回程（breadcrumbs）**：自 takeoff 完成起按 0.5~1m 记录降采样轨迹
  （只存 LIO/odom 系，同帧回放对消绝对漂移）；返航 = 就近接入 + 倒序喂 traj_server。
- **关键语义**：RETURN 产生的是 **Safety trajectory**，不进 Mission 状态机；
  `task_dispatcher` 永远不能覆盖 RETURN（直接解决 H5 环路）。
- **段级 watchdog**：单段超时 → §9.1 微修复重试 1 次 → 失败 → 就近落安全区（H4 迫降点库）。
- **距离感知能量**：剩余回放路径长 × 消耗系数 < 电量余量，不够就就近降落（H7）。
- **机库收口**：home 到位 → 净空核验（H3 多源触地）→ L4a/L4b → `RETURNED_LANDED` + `safety_hold`。
- 不变项：home 快照于 auto_switch 完成时刻；**禁用 `VEHICLE_CMD_NAV_RETURN_TO_LAUNCH`**
  （现有 `rtl()` 改绑或删除）。

### 9.3 原地降落（L4）：V-A 已有，V-B 盲降新建
- **V-A（位置有效）**：现成 `trigger_landing()`→AUTO_LAND→降落监护。不动。
- **V-B（EV 已死/将死、心跳链路活）状态机 `LAND_IN_PLACE`**：
  1. 进入条件：§10 快通道 RED 确认；或 C1/B4 等待放弃超时后降级进入。
  2. 同一拍先停发 EV（§10 RED 序列），心跳切 velocity=True 零速悬停 2s。
  3. 混合 setpoint：`position=[x,y,*]`（锁最后水平位）+ `velocity_z=+0.4m/s`（NED 下正）下降，心跳全程保持。
  4. 触地多源投票（H3）：baro 高下降率归零 + vz≈0 + 测距触地，三取二。
  5. 触地后：`OFFBOARD_STATE=DISABLED` + `LANDING_VB→LANDED` 上报 + 1Hz 强制 disarm + `safety_hold=True`。
- 承认风险：盲降对脚下无感知；0.4m/s 限速压动能；只在已知净空区（重放沿途/主干道/机库前）信任它。
- 时序锚点：V-B 与「停发 EV」同拍启动，与 §6.1 的 4.5s 失效窗口赛跑；竞速失败→L5 自动接管。

### 9.4 航点速度剖面（latched `/goal_vel_max`，goal 通道零改动）
- `PoseStamped /goal_pose_3d` 不变；新增 latched `std_msgs/Float32 /goal_vel_max`。
- 时序纪律：**先写速度、后发 pose**；traj_server 收 goal 时快照 `min(值, 机载硬顶)`。
- 完全不发 = 默认保守值（调度层零改动可跑）。
- 机载只保留：①硬顶（`MPC_XY_VEL_MAX` 再压一道）；②非法值防御（≤0/NaN→默认+`PARAM_IGNORED`）。
- 连带：§9.1 停滞判定与 H7 能量计算按分段限速折算（否则引入新误报）。

## 10. LIO health gate：双通道设计（快慢分离，一票否决 vs 零动作）

- **快通道（→RED，≤0.5s，一票否决）**：只收「必坏物理事件」——

  | 事件 | 为什么「必坏」 |
  |---|---|
  | odom 全停 >1s | 进程/驱动级停止，无解释空间 |
  | sanitize 计到 NaN/Inf 协方差 | ESKF 求逆已发散（0914 前科） |
  | 消息时标倒退 / 单步跳变 >1s | 时钟或内部状态坏 |
  | 跳变守卫连续 2 次 | 估计器瞬移而非漂移 |

  协方差趋势斜率、创新统计、滑窗均值等「趋势检测」**不进快通道**（高误伤源头）。
- **慢通道（→YELLOW，只限任务、零动作）**：单帧 stale/单次跳变/协方差短时贴顶/z 交叉差…
  → 上报 + 禁返航 + 计入错误预算，永不直接切断。
- **RED 动作序列**：①立即停发 EV（本函数 return，连 NaN 不发）；②心跳切 velocity 零速 + 同拍启动 V-B；
  ③上报 `LIO_RED` + safety_hold。RED 单向闩锁（本 flight 不回退）。
- 低概率≠低处置：真发散罕见，快通道阈值只认「必坏」，不需统计放大器。

## 11. PX4 参数兜底（最终独立 failsafe，与软件层并列）

| 参数 | 现值 | 建议 | 理由 |
|---|---|---|---|
| `COM_OF_LOSS_T` | **60** | 2 | 断链 60s 才判 lost；位置有效断链会挂 60 秒 |
| `COM_OBL_RC_ACT` | 4 | 位置可保持类（Hold/Position） | 入向断时飞控端唯一自动防线 |
| `COM_QC_ACT` / `COM_FLTT_LOW_ACT` | Return 系 | Land | 无全局定位，Return 永不可达 |
| `MPC_Z_VEL_MAX_DN` | 1.0 | 0.7 | 压低 L5 漂移下降动能 |
| `EKF2_EV_* / OF / GPS` | 已定档 | 维持 | 0918 证明参数体系无罪 |
| `GF_ENABLE`/`GF_*` | 默认 | **关** | 无 global 时围栏只剩误伤 |
| `NAV_DLL_ACT` | — | **仅 Warn（0）** | 数据链断开不抢控制权（机载租约 P-B 自行判定） |
| `COM_DISARM_LAND` | 2 | 保留 2 | 配合稳定接地判据防假接地空中关车 |
| `FD_*` | 默认 | 保留 | IMU/执行器硬失效归 FMU 自理 |

**明确拒绝「关闭 PX4 一切竞争 failsafe」**：PX4 参数阶梯是 comms 共模死亡时
**唯一**防线（L5）。正确姿势 = 逐项对齐审计：每条标注 {保留/改值/关闭+理由}，
触发动作与本机降级阶梯兼容（要求位置的一律换 Land/Descend 系）。

## 12. 资源隔离与 OS 护城河（安全架构的一部分，非运维细节）

**软件 watchdog 不能与被监控对象共享同一资源故障域**（0918 已实锤 executor 饥饿）。

核预算分配（P1 基线，真机 `lscpu -e` 校准后定稿）：

| 池 | 成员 | 原则 |
|---|---|---|
| 5,6（大核） | **ego_planner_node + traj_server** | 规划独占 |
| 7（大核） | **comms** + livox 驱动（从 5 挪走） | 控制面独占；supervisor V-B 接管同池 |
| 4（大核） | super_lio 主 + lidar_localization | 定位独占 |
| 0-3（小核） | agent、tf、supervisor、task_dispatcher、媒体/上传/algo | 非实时全家，nice/cgroup 限峰值 |

- **supervisor 轻量化**：订阅全轻量话题（odom BE depth1、safety_state 5~10Hz、
  status 1Hz、事件稀疏、B1 探针 0.5Hz），总占用 <10% 一个大核；**禁止绑进 5,6**。
  它应像「飞行安全 PLC」，不做 LIO/点云匹配/复杂规划/大 JSON/日志/媒体。
- `/planner_diag` 只允许 1Hz 聚合发布。
- **CPUQuota/OOMScoreAdjust/SCHED_FIFO 必须逐进程生效**：当前 `ego_planner.service`
  一个 shell 拉起全栈，cgroup 会罩住整栈 → P1 必须先做每进程单元化（systemd 模板/run 包装）。
- 任务期媒体纪律：pcd 分片上传串行化到落地后；RTSP 飞行中只预览或 rkmpp 硬编；snap 限频；
  super_lio 生产关 PCD 写盘；algo 推理限 ingActions 之外。

## 13. 与外部调度层接口契约（单出口协议）

外部调度层零改动也可运行；以下为系统单方面广播 + 共研协议项（**不新增自定义 msg**）。

- `/mission_status`（String+JSON，1Hz 稳态+事件即发）：`goal_seq/state/reason/dist_goal/alt/battery/lio/ts`，
  `state` 全序，兼作 comms 心跳（外部「断流 3s=失联」，H8）。
- `/mission_event`（String+JSON）：`GOAL_SUBSTITUTED/GOAL_UNREACHABLE/RETURN_INIT/RETURNED_LANDED/LIO_RED/...`。
- 共研协议项：**P-A** `/goal_vel_max`（latched Float32）；**P-B** `/mission_lease` 双向租约；
  **P-C** 外部消费 `RETURNED_LANDED/SAFETY_HOLD` 后程序自动 `RESET_HOLD`；
  **P-D** 到达确认 status 驱动（`ARRIVED/ARRIVED_UNACK/GOAL_SUBSTITUTED`，带 seq+距离）。
- `safety_hold` 再起飞闸门：本地安全动作（L3'/L4a/L4b/B1~B4）执行后 hold=True，
  goal 一律不转发；任务类上报（GOAL_STALLED）不进 hold。

## 14. 任务面（task_dispatcher）改造清单（代码取证确认）

### 14.1 Offboard 多写者（最高优先级，P-ARCH）

全量写入方清单见 **引.2.1**。要点：

- **活的旁路写者**：
  - `UavMissionManager`（[L48-64](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_manager.py#L47-L64)）
    ——由 `RemoteControl` 在 [remote_control.py#L41](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/remote_control.py#L41)
    无条件实例化（`TaskDispatcherNode.__init__`），并有 `create_timer(0.1, ...)`
    持续推 setpoint/心跳；**另发 `/goal_pose_3d`**。
  - `UavActionManager`（[L36](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_action_manager.py#L35-L38)）
    ——由 `CruiseUavTaskManager` 在 [cruise_uav_task_manager.py#L108](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/cruise_uav_task_manager.py#L108)
    实例化，直写 `/fmu/in/vehicle_command`。
- **潜伏写者**：`uav_mission_server`（xtd2_control，`setup.py` 未注册入口）、
  `UavMissionCore`（[L40-56](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_core.py#L40-L56)，当前无调用方）。
- **命令链真相**：云端 `takeoff`/`land` → `RemoteControl._call_uav_takeoff/_call_uav_land`
  → `UavCommandSender` 发 `/uav_command`（`std_msgs/String`，无 schema/ack）
  → 被 `UavMissionManager`（活）与 `uav_mission_server`（潜伏）同时订阅
  → **直接写 FMU**，完全绕过 planner 与 comms 闸门。
- **规范通道空置**：`XTD2Cmd` 服务（`/xtdrone2/<ns>/cmd`）已由 comms 提供，但**无调用方**。

→ 改造：`/fmu/in/*` 全收归 comms；三处旁路 publisher 全部删除；云端飞行意图统一经
`XTD2Cmd`（带 ack/seq）落到机载 Mission Manager；四份状态机副本退役为**单份**。

### 14.2 航段无超时

`perform_action_walk_to_pos_3d`（[perform_action_manager.py#L180-L280](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/perform_action_manager.py#L180-L280)）
为 `while True` + `time.sleep(0.2)`（20Hz 刷 `/goal_pose_3d`）+ 内层 `while True` 自测距离判到达，
**无任何超时** —— 0918「悬停至耗尽电量」的真实机制。
→ 飞段成败以机载事件为准（§13），删除调度侧独立到达判定。

### 14.3 返航/disarm 双链

`handle_disarming_state`（[uav_mission_manager.py#L453-482](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_manager.py#L453-L482)）
循环 `land()+disarm()`，与机载 L4a/L6 降落监护形成双通道抢 disarm。
→ 动作权归机载（L3'/L4），disarm 机载独占；调度只保留返航后编排（关舱/充电/上报）。

### 14.4 可复用的现存资产（不必推倒）

- `ros2_topic_subscriber` 已订阅 `battery_status` / `failsafe_flags` / `vehicle_status` /
  `local_position` / `attitude` → 机载缺的 battery/failsafe 信号**在调度侧已有**，
  可作为过渡期的观测源（但权威判定仍应在机载 §5）。
- `XTD2VehicleState` + comms 的 `debug/vehicle_state` 10Hz 快照 → `/mission_status` 的现成素材。
- `preflight_check_node` 已具备检查逻辑，仅缺 READY gate 接入（H1/P10）。

目标四层职责切分（与 §1 对齐）：

| 层 | 载体 | 负责 | 禁止 |
|---|---|---|---|
| 云端 | MQTT | 任务发起/暂停/撤销/遥测/KeepAlive | 秒级闭环 |
| 任务编排 | task_dispatcher | 航点序列/dock/事件桥接/服务管理 | 写 `/fmu/in/*`、自行判飞机状态 |
| 安全监督 | xtd2_safety | 健康门/预算/仲裁/确认链/事件单出口 | 任务决策 |
| 控制面 | multirotor_communication | FMU 唯一写通道 | 安全决策 |

## 15. 实施顺序（v0.5 重排，按 GPT ①~⑥）

| 阶段 | 内容 | 落点 | 验收 |
|---|---|---|---|
| **P-ARCH** | **① FMU 单写者**：调度侧删 `/fmu/in/*` 直发，MQTT→`XTD2Cmd` | task_dispatcher | 仿真飞行中云端连发 takeoff/GOTO/land，FMU 写入方始终唯一；**必须先于一切授权** |
| P0 | §11 参数 + PX4 failsafe 逐项对齐审计 | QGC | 台架拔线：位置有效断心跳 ≤3s 进 Hold |
| P1 | ② 资源隔离：comms 控制面独立线程 + SCHED_FIFO/OOMScore/UDP 缓冲 + 每进程单元化 + 核分配 | comms + systemd/launch | CPU 100% 压测心跳 ≥18Hz；杀非关键进程控制面无损 |
| P2 | **③ Authority/状态机**：仲裁优先级（只升不降）+ 安全动作确认链 REQUEST→ACK→EFFECT→CONFIRMED + 状态机单向规则（接口/枚举先行） | 设计+头文件 | 评审通过：每个安全动作有「生效确认」观测量 |
| P3 | **② Safety Supervisor 独立进程**（xtd2_safety 影子模式）：health gate 四级/预算/仲裁/接管单出口 | 新包 | 与 comms 并行真飞 ≥10 架次：决策一致性 + 误报率数据 |
| P4 | supervisor 信号扩展：battery/推力饱和/跟踪误差/LIO↔pcl 互核/clip_count/时基单调；B1 探针 + agent 优雅重启；机库磁干扰基线 | xtd2_safety | 台架注入各信号→对应级别触发；探针误报=0 |
| P5 | 授权第一步·放行权：comms auto-switch/goal 转发 gate 于 supervisor clearance；pet-the-dog 互活；TAKEOFF_ABORT_WINDOW | 双节点 | 未 READY 发 goal 被拒 NOT_READY |
| P6 | 授权第二步·V-B 执行权：supervisor override + comms 退避协议；降落链加固（stable_ground_contact/spool-down/触地投票含电流） | supervisor+comms | 系绳台杀 LIO ≤3.5s V-B；假接地不 disarm |
| P7 | **④ Flight epoch/generation + 任务面**：goal 进展 watchdog（分段限速）+ 备用点微修复 + `/planner_diag` + goal 幂等去重 + epoch 全覆盖 | traj_server+supervisor | 复演 0918「悬停 56s」：≤T_goal 内替代或 UNREACHABLE；低速段零误报 |
| P8 | 任务面 2：错误预算 + 重放回程（记录过滤/z 钳制/偏出>3m 重规）+ `RETURNED_LANDED` | traj_server+supervisor | 连错 2 个 goal → 沿去程退回机库降落 |
| P9 | 调度层共研包：P-A/P-B/P-C/P-D（goal 通道零改动、无自定义 msg） | 调度层+机载 | 先速度后 pose 时序 seq 对账无误、租约超时链正确 |
| P10 | pre-flight READY gate 全量（H1）+ 多机 unicast 模板 + dock 联锁 + initialpose 自动注入 | launch+supervisor+dispatcher | 开机不收敛→goal 被拒且 `NOT_READY{missing}` |
| P11 | 归档/迭代 | 本文 | — |

依赖说明：**P-ARCH 必须先于 P5/P6**（clearance/override 上线前先收权，否则手动通道就是绕过 supervisor 的后门）；
P1 是一切检测/执行可靠性的前提；P3 影子先行、P5/P6 两步授权（误报率数据 = 授权许可证）；
P9 可与 P5~P8 并行，但 P-A 需赶在 P7 判定逻辑冻结前（否则 P7 先用默认限速值）。

## 16. 明确不做 / 边界

1. 不动外部调度层的任务取舍（只广播状态供消费）。
2. 系统内不建 waypoint 队列（与调度层职责重复）。
3. 不使用 PX4 RTL / 不假装可达 AUTO_RTL。
4. EV 关通道时机：YELLOW 绝不关；RED（快通道「必坏」确认后）必须同拍关。
5. FMU 空中重启软件不可救（D4），归硬件整改。
6. 不引入新 msg 包：上报全 `std_msgs/String`+JSON。
7. 不做统计趋势型自动切断（趋势信号只入慢通道/预算）。
8. 不把 Super-LIO 逻辑塞进 supervisor（时序量级不同，混入会拖垮检验层）。
9. 不把 supervisor 做成「第二个 comms」——它是「飞行安全 PLC」，不是第二套无人机软件。

## 17. 无人化隐患清单（H1~H11，重归类为能力/权威映射）

| # | 隐患 | 映射到新模型 | 对策 |
|---|---|---|---|
| H1 | 开机无自检闸门 | READY 未达成 | pre-flight READY gate，拒 goal 报 `NOT_READY{missing}` |
| H2 | 外部调度层自身死掉 | TASK_LAYER_ALIVE=0 | `/mission_lease` 双向租约→MISSION FREEZE→HOVER→RETURN/LAND |
| H3 | 触地判定单源（假接地空中关车） | GROUND_CONTACT_VALID 误=1 | 触地三取二投票 + 电流第四票 |
| H4 | 返航失败无第二降落场 | RETURN_PATH_VALID=0 | 安全区库，就近迫降 |
| H5 | 软件环拍（跳过→返航→再起飞） | 状态机可回跳 | 单向闩锁 + 重试预算收敛到 {悬停,降落,上报} |
| H6 | 日志/磁盘水位阻塞 rclpy | CONTROL_LOOP 被共模拖垮 | 磁盘水位入 READY+慢通道；生产关 PCD；journald 限速 |
| H7 | 电量阈值不感知距离 | BATTERY_VALID 与 RETURN_PATH 联动 | 距离感知能量，不足则就近降落 |
| H8 | 失声即故障（comms 崩无感知） | 无人站失去决策依据 | `/mission_status` 1Hz 兼心跳；offboard-loss 阶梯兜底 |
| H9 | 环境慢漂移 | LOCAL_MAP_VALID 渐进劣化 | 匹配/密度趋势只进慢通道→`MAINTENANCE_HINT` |
| H10 | 时基未闭环 | 快通道判定整体失效 | S10 真机闭环；comms 时标单调自检；chrony 基线 |
| H11 | 多机/多站点同域 | 异图/双头控制 | 每场地 domain/ns/地图指纹三元组入 READY gate |

## 18. 版本与讨论状态

- v0.2：故障补丁型（遮挡备用点/错误预算+重放返航/上报通道排查/LIO 双通道）+ H1~H11。
- v0.3：进入安全监督架构（xtd2_safety 独立进程影子→两步授权；航点速度剖面；PX4 failsafe 审计）。
- v0.4：发现任务层控制权冲突（调度层纳入架构；offboard 双写收权 P-ARCH 前置于授权）。
- v0.4.1：发现 OS 资源隔离也是安全架构（3588 资源预算与防阻塞审计）。
- **v0.5（本版）**：切换为 **Architecture Refactor**。确立 ①Authority 模型、②Capability 模型、
  ③Safety 状态机（单向）、④Flight epoch/generation、⑤单一 FMU 写者、⑥安全 override 仲裁、
  ⑦资源隔离、⑧命令确认模型、⑨Mission/Safety 分离、⑩PX4 最终独立 failsafe 十项，
  并把已有 H1~H11 / A1~D5 / G1 全部重映射到这套架构上。
- 下一版触发条件：P3 影子模式误报率统计回填（授权门槛）、真机新事件复盘、
  或机库磁干扰基线超标（G4 升权）。

---

## 附：本次代码取证要点（2026-09-23）

取证范围：`xtd2_ws/XTDrone2_ego_planner`（comms / planner / launch / lio / msgs / control）
+ `uav_service/src/task_dispatcher`。模块级清单见 **引.2**，FMU 写入方全量清单见 **引.2.1**。

1. **comms 单点超载**：`multirotor_communication.py`（2089 行）单节点承担 FMU I/O + TF + auto-switch +
   goal 转发 + 降落监护 + 温度 + EV 转发 + 协方差 sanitize + 跳变守卫，**无** `/mission_status`、
   `goal_seq`、battery 订阅、V-B、supervisor、`safety_hold`、`/goal_vel_max`（均为提案，未落地）。
2. **offboard 多写者（3 活 + 2 潜伏）**：
   - 活：`UavMissionManager`（[L48-64](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_manager.py#L47-L64)，
     经 `remote_control.py` [L41](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/remote_control.py#L41) 实例化）、
     `UavActionManager`（[L36](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_action_manager.py#L35-L38)，
     经 `cruise_uav_task_manager.py` L108 实例化）。
   - 潜伏：`uav_mission_server`（[uav_mission_server.py](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_control/xtd2_control/keyboard/uav_mission_server.py#L37-L53)，
     `setup.py` 未注册入口）、`UavMissionCore`（[L40-56](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_core.py#L40-L56)，无调用方）。
   - 合法：`obstacle_distance_publisher`（仅 `obstacle_distance`，CP 专线）。
3. **命令链**：`/uav_command`（`std_msgs/String`）发布方 3 处（`remote_control` / `perform_action_manager` /
   `cruise_uav_task_manager`），订阅方为该两个状态机副本 → 云端 takeoff/land **直写 FMU**；
   而规范通道 `XTD2Cmd` 服务（comms 已提供）**无调用方**。
4. **航段无超时**：[perform_action_manager.py#L180-L280](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/perform_action_manager.py#L180-L280)
   `while True` + `time.sleep(0.2)` + 内层 `while True` 自测距离判到达，无超时。
5. **返航/disarm 双链**：[uav_mission_manager.py#L453-L482](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/uav_mission_manager.py#L453-L482)
   循环 `land()+disarm()`。
6. **goal 双入口**：comms 在 [L277-281](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_communication/xtd2_communication/multirotor_communication.py#L274-L281)
   收 `/goal_pose_3d` 并（offboard 就绪后）转发 `/move_base_simple/goal`；
   而 `xtd2_traj_server` 在 [L867-870](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_third_party_pkgs/motion_planning/ego-planner-swarm/src/planner/plan_manage/src/xtd2_traj_server.cpp#L866-L870)
   **直订 `/goal_pose_3d`（不受闸门约束）**。
7. **状态机四份副本**：`UavMissionManager` / `UavMissionCore` / `uav_mission_server` / comms `auto_switch_to_egoplanner`。
8. **资源现状**：控制面与规划共池 `taskset -c 5,6`；`xtd2_traj_server`、`lidar_localization`、
   媒体/上传/algo 均未绑核（见 [ros2_single_vehicle_demo_launch.py](file:///home/ywj/git/xtd2_ws/XTDrone2_ego_planner/xtd2_launch/launch/ros2_single_vehicle_demo_launch.py#L87-L113)）。
9. **开机时序无闸门**：靠 `wait_for_px4_odom` 话题轮询 + `TimerAction(5s/7s)` 延时；
   `preflight_check_node` 的定时器在 [task_dispatcher_node.py#L234](file:///home/ywj/git/uav_service/src/task_dispatcher/task_dispatcher/task_dispatcher_node.py#L234) 被注释。
10. **可复用资产**：调度侧 `ros2_topic_subscriber` 已订 `battery_status`/`failsafe_flags`；
    `XTD2VehicleState` 已有状态快照 msg；`XTD2Cmd` 通道已就绪但空置。
11. 速度语义：`remote_control.py` / `cruise_uav_task_manager.py` 各有 `speed_map`（四足遗留），
    UAV 档需另立表（§9.4/P-A）。
