# PX4 失控事故复核结论与参数清单（v2）

| 项 | 内容 |
|---|---|
| 日志 | `0911/05_46_38.ulg`（12,304,510 B，有效数据 294.8 s） |
| 飞控 / 固件 | PX4 FMUv6C (STM32H7)、Nuttx、**PX4 v1.15.4**（`ver_sw 99c4040…`） |
| 定位方案 | 激光 SLAM（SuperLIO）经 **uXRCE-DDS** 接入（`timesync_status.source_protocol = 2 = SOURCE_PROTOCOL_DDS`）；`EKF2_GPS_CTRL = 0`，GNSS 不参与解算 |
| 起飞点 | lat 40.28019 N, lon 122.15639 E |
| 复核日期 | 2026-09-11 |
| 与旧报告关系 | 本文**修正**旧报告 4 处结论（见 §5），其余（RC failsafe 停发杆量 96.7 s、kill switch 延迟 78.8 s、保护链被人为关闭、推进/电池正常）经独立复算**成立** |
| 时间基准 | 全部 `t` 以日志首帧为 0；解锁在 `t = −4.83 s`。**事件时刻一律采用 `timestamp_sample`（EKF 真值时刻），不用 `timestamp`（发布/落盘时刻）**，原因见 §3.1 |

---

## 1. 结论摘要（修订版）

**直接死因：`reset_vel_to_flow`。** 在视觉里程计一次约 150 ms 的**到达中断**窗口内，EKF2 重新启用了早已超出自身量程、且全程持续输出物理不可能数据的光流，并**用光流"重置"（而非融合）水平速度状态**，写入 −42.5 / −12.8 m/s（\|v\| = 44.4）的幻影速度。MPC 随即满推力、满倾角去"刹车"一个不存在的速度，飞机以 13~15 m/s 平飞逃出 250 m，26 s 后坠地。

责任划分（**与旧报告的关键差异**）：

| 环节 | 判定 | 一句话依据 |
|---|---|---|
| 气压计 | **正常**，全程唯一稳定在线的高度源，事故中既未触发重置也未被拒绝 | `error_count≡0`；`cs_baro_hgt` 100%/0 次切换；`baro_vpos` 测试比全程 ≤0.2 |
| 激光 SLAM（SuperLIO）**内容** | **正常**，事故瞬间与飞跑段都与 GPS 独立真值吻合 | 反解速度模与 GPS 差：事故前 0.11 m/s、飞跑段 **0.99 m/s** |
| 激光 SLAM **到达性** | 有中断（**这是触发条件，不是内容错误**） | `vision_data_stopped` 全程 10/11 次，事故那次恢复用时 142 ms |
| **光流** | **全程脏数据源（直接肇事者）** | 90 个"机体不动而流角速率 >0.5 rad/s"的物理不可能样本；肇事样本 1.704 rad/s（97.6°/s）而机体 \|w\| 仅 20.6°/s |
| **EKF2 决策** | **致命环节** | `cs_opt_flow` 在 EV 停摆的瞬间 0→1，紧接 `reset_vel_to_flow` |
| 速度状态可修复性 | **结构性缺陷** | `EKF2_EV_CTRL=11` 缺 bit2 → `cs_ev_vel` 全程 0；`EKF2_GPS_CTRL=0` → 无任何正确数据能改写速度 |
| 激光测距（定高/地距） | **超量程早归零**（旧报告结论成立，但危害路径被修正） | 13.92 s 在真实高度 7.2 m 时归零，声明量程 12 m |
| RC 链路 / 保护链 / kill switch | 旧报告结论成立 | 见 §5 与旧报告 |

**关键因果顺序（旧报告写反的一处）**：机体大角速率（262.9°/s @ `t=87.482`）发生在速度重置**之后约 140 ms**，是 MPC 抢救幻影速度的**结果**，不是诱因。重置前（86.8~87.12 s）机体 \|w\| 峰值仅 **37.1°/s**。

**事故不是孤例**：同一链条在 **t = 59.048 s 已完整复现一次**（`vision_data_stopped → +10 ms reset_vel_to_flow → +110 ms reset_pos_to_vision`），只因当时只有备用 EKF 实例中招、主实例未受影响而被埋没。87 s 那次是两实例同时中招 + 主实例恰好在 87.37 s 切换。

---

## 2. 修订后的事故时间线（全部为原始行、零插值）

```
 时刻(EKF真值)   事件 / 量                                                  数值
--------------------------------------------------------------------------------------------
 ≤86.745        EV 创新 ev_hvel=0.168 m/s、ev_hpos=0.053 m，测试比 0.22/0.01   ← SLAM 完全正常
 86.818         光流未补偿角速率 0.000 rad/s                                  ← 光流正常
 87.058         光流未补偿角速率 0.852 rad/s(48.8°/s)  |w|=8.8°/s  模块gyro=0.8°/s  ← 脏样本①
 ~87.0          cs_ev_pos 1→0（EV 停止融合）
 87.119         ★reset_vel_to_flow★  写入 vx=−42.54, vy=−12.83, |v|=44.43
 87.12~87.24    cs_opt_flow = 1（仅 120 ms；60~86.8 s 期间一直是 0）
 87.237         光流未补偿角速率 1.704 rad/s(97.6°/s)  |w|=20.6°/s  模块gyro=1.1°/s  ← 肇事样本
                换算校验: 1.684 rad/s × HAGL 28.87 m = 48.6 ≈ 实测 vel_ne_x −49.31
 87.240         ★reset_pos_to_vision + starting_vision_pos_fusion★
                同一时刻 ev_hvel 创新 = −44.887（= 错误状态 − 正确视觉，是结果不是原因）
 87.384         vehicle_local_position 首次显示 vx=−42.54（输出预测器滞后 ~220 ms）
 87.482         机体 |w| = 262.9°/s（p=−245°/s）  ← 旧报告误记为 t=87.0 的"突跳"
 88.1~113       thrustZ = −1.00（100% 饱和）持续；thrust_setpoint_achieved = 0
 100.47         第二次 reset_vel_to_flow → vx 由 −30 翻为 +25.8；此后飞机开始掉高度
 104.9          GPS 距起点最远 254.0 m，随后朝返航方向回飞 ~45 m
 113.704        ★撞地★ |w| = 2314.9°/s（全程最大），位置距起飞点 214.5 m
 113.06~209.56  input_rc.rc_failsafe = 1；manual_control_setpoint 停发 96.74 s
 210.02         飞行员首次拨 kill_switch=ON + arm_switch=DISARM
 288.85         manual_lockdown 才置位（比首次指令晚 78.83 s）；293.85 armed=0
```

---

## 3. 推断依据与取证方法

### 3.1 必须先处理的双时间基准

PX4 的 `estimator_*` 系列话题，其 `timestamp`（发布/落盘）系统性晚于 `timestamp_sample`（EKF 周期真值时刻）：

| 话题 | pub − sample 中位 | p5 ~ p95 |
|---|---|---|
| `vehicle_local_position` | 0.8 ms | 0.4 ~ 3.7 |
| `vehicle_attitude` | 0.5 ms | 0.2 ~ 3.2 |
| `estimator_status_flags` | **161.4 ms** | 135 ~ 228 |
| `estimator_innovations` | **184.4 ms** | 137 ~ 236 |
| `estimator_event_flags`（有事件的行） | **222 ~ 229 ms** | — |

**影响**：旧报告与本文第一轮分析所用的 87.32 / 87.34 / 87.47 全是发布时刻，统一偏晚约 220 ms。改用 `timestamp_sample` 后得到 87.098 / 87.119 / 87.240，因果顺序随之纠正。
**另一个坑（本文第一轮犯过的错，记录以免重犯）**：`estimator_innovations` 只有 2 Hz（间隔 ≈0.5 s），按 0.1 s 分箱取最大值并对空箱做线性插值，会在两个真实采样点之间凭空造出一条"86.8 起持续劣化"的假斜坡。**任何 0.1 s 级因果判断都必须用原始行。**

### 3.2 判据：把 EV 创新反解回 SLAM 测量值，与 GPS 比对（免坐标系对齐）

ECL 约定 `innovation = pred − obs` ⇒ `obs = state − innov`。SuperLIO 的速度 = `EKF速度 − ev_*_innov`。只比**模长**以规避本地帧与 GPS 帧的旋转差异。

| 段 | EKF 状态 vs GPS（\|v\| 差中位） | **SLAM 反解 vs GPS** | 反解用错符号(+innov) |
|---|---|---|---|
| 事故前 0~86.8 s | 0.10 m/s | 0.11 m/s | 0.11 |
| **飞跑段 87.4~112.5 s** | **13.65** | **0.99** | 57.57 |
| 坠地后 116~230 s | 0.33 | **123.4** | 123.1 |

飞跑段逐点（m/s）：

```
 t=87.47   |EKF|=44.02   SLAM=3.71   GPS=3.00
 t=90.97   |EKF|=34.29   SLAM=13.26  GPS=13.29   ← 差 0.03
 t=94.97   |EKF|=33.83   SLAM=13.57  GPS=13.17
 t=97.81   |EKF|=28.96   SLAM=14.88  GPS=14.77
```

**读法**：飞跑段 SLAM 报的速度与 GPS 实测速度差 <1 m/s，而 EKF 自己错了 13.7 m/s → SLAM 是对的。坠地后符号反过来（EKF 与 GPS 一致、SLAM 差 123 m/s）→ **odom 确实是撞地之后才漂的**，与飞手现场观察一致。

### 3.3 为什么"打满杆也拉不回"

| 检查项 | 实测 | 含义 |
|---|---|---|
| `cs_ev_vel`（视觉速度融合） | **全程 0.00** | `EKF2_EV_CTRL=11` = 1+2+8，缺 bit2(=4) → 唯一可信的速度参考进不了 EKF |
| `cs_gps` / `cs_gps_hgt` | 0.00 / 0.00 | GNSS 也不可用 |
| `xy_reset_counter` 递增（87~113 s） | **27 次**（≈每 1 s） | 位置被正确数据每秒拉回 |
| `vxy_reset_counter` 递增（87~113 s） | **5 次**，其中 2 次是脏的 flow reset、3 次是主实例切换 | **速度一次都没被正确数据修过** |
| 主实例切换（全程） | **6 次**：87.37→1 / 89.03→0 / 97.38→1 / 99.44→0 / 126.43→1 / 184.48→0（与旧报告"7 次"的差异是 `instance_changed_count` 含初始指派） | 每次切换伴随一次 vxy/z/heading 全状态重置 |
| EKF−GPS 速度误差（87.8~99.8 s） | **40~44 m/s 恒定，12 s 不衰减** | 一个常值偏置；IMU 递推只能改增量 |
| 杆量 | `manual_control_setpoint.valid` 全程 1，pitch 长期 = **+1.00**、roll 达 **−1.00** | **输入通路是通的**，不是"打杆没反应" |
| MPC 设定值 | `spVx` 被限幅在 **−4.84 ~ −5.00**（`MPC_XY_VEL_MAX=5`） | 速度环误差 = (−5) − (−33) = **+28 m/s，符号永不翻转** |
| 执行器 | `thrustZ=−1.00` 连续 25 s；`thrust_setpoint_achieved=0` 持续 | 需求无法实现 |
| `eph` / `evh`（飞跑段） | ≤**1.18 m** / ≤1.53 m/s | **置信度虚高**，地面站无任何告警 |

机制：`startFlowFusion()` 里 `else if (!isHorizontalAidingActive()) resetFlowFusion();` —— **EV 一停，水平辅助源数=0，光流"启动"就必然等价于"重置速度"**；而 `resetHorizontalPositionTo()` 只改位置不改速度。所以 EV 越不稳定，光流越有权改写速度。

### 3.4 丢帧是偶发还是持续

| 阶段 | `cs_ev_pos` 在线占比 | 离线条块数 / 总时长 | `vision_data_stopped` |
|---|---|---|---|
| 事故前 −5~86.8 s | **98.4%** | **1 块 / 0.03 s** | inst0 **1 次** @9.556 / inst1 **2 次** @9.555, 59.048 |
| 失控 86.8~113.5 s | 80.9% | 5 块 / 0.29 s | 各 2 次 |
| 坠地后 113.5~294 s | 85.9% | 21 块 / **71.88 s**（最长 70.53 s） | 各 7 次 |

EV 每次中断的恢复用时高度一致：**110 / 112 / 142 ms** ≈ 1~1.5 个 10 Hz 周期。
→ **事故前属偶发；"一直在发生"只出现在坠地之后，那是后果。**
但 `EV_MAX_INTERVAL = 200 ms`（`EKF/common.h` 常量），**10 Hz 只有 2 帧余量**，属结构性脆弱。

DDS 桥本身健康（排除链路停顿与时钟跳变）：`timesync_status` 1 Hz，**>1.5 s 空洞 0 次**；RTT 中位 4.56 ms / p95 7.52 / max 10.36；`estimated_offset` 逐帧变化中位 0.159 ms、**无任何 >1 ms 阶跃**（即不存在"时间戳倒退导致样本被判为旧帧"）。

未决项（需现场确认）：`telemetry_status[1]`（5760、`mavlink_v2=0`）rx ≈ **7400 msg/s 恒定**，`rx_packet_drop_count` 全程累计 **+1705**（249/293 行都在涨），t≈241 s 后 rx_rate 崩到 1；`telemetry_status[0]` 在 t≈121~240 s 完全 rx=0。因 SLAM 走 DDS（`source_protocol=2`），这两条 MAVLink 口的丢包**不能直接归因给里程计**，须核对 `UXRCE_DDS_CFG` 与串口分配。

### 3.5 为什么配置正确的 `SENS_FLOW_MAXHGT` / `MAXR` 没能拦住（源码级）

| 参数 | 实际值 | 在 v1.15.4 中的真实作用 |
|---|---|---|
| `SENS_FLOW_MAXHGT` | 5（默认 100） | **不参与估计器判定。** 全仓唯一读取点 `VehicleOpticalFlow.cpp:243-272`，只是把"参数"与"驱动自报量程"取 min/max 后**转发**进消息字段；参数原文语义是给控制器**限高度设定值**用（"The height setpoint will be limited to be no greater than this value…"）。`src/drivers/` 与 `src/modules/ekf2/` 检索 `SENS_FLOW` 均 **0 命中** |
| `SENS_FLOW_MINHGT` | 0.2 | 同上 |
| `SENS_FLOW_MAXR` | 8 | **唯一真正生效的光流门**（`optical_flow_control.cpp:54-55`），但只检查**未补偿** flow_rate 的 2D 模 |

实测门限有效性：

| 量 | max | p99 | 超 8 rad/s | v1.15.4 是否检查 |
|---|---|---|---|---|
| **未补偿** \|flow_rate\|（门限用的就是它） | **7.948** | 6.649 | **0 / 1096** | ✅ 是 |
| **补偿后** \|flow_rate\|（算速度用的是它） | **25.420** | 7.138 | 7 / 1096 | ❌ 否 |

- 反查 `L68`（判废时把 `_flow_rate_compensated` 置 0）：**整飞行 1096 个光流样本，被这道门作废的次数 = 0**；峰值 7.948 距 8.0 只差 0.6%。
- 肇事样本未补偿模 = hypot(1.704, 0.532) = **1.785 rad/s，仅门限的 22%** → 必然放行。

`controlOpticalFlowFusion()` 启/续条件全集中**没有任何"地距超上限就不许用"的判据**：

```cpp
L49  min_quality = in_air ? _params.flow_qual_min : _params.flow_qual_min_gnd;   // EKF2_OF_QMIN=1
L53  is_quality_good   = (quality >= min_quality);
L54  is_magnitude_good = flow_rate.isAllFinite() && !flow_rate.longerThan(_flow_max_rate);
L56  is_tilt_good      = (_R_to_earth(2,2) > _params.range_cos_max_tilt);
L82  terrain_available = isTerrainEstimateValid() || isRecent(地形测距融合, 10e6);   // 只查新鲜度
L84  continuing = (_params.flow_ctrl == 1) && tilt_align && (terrain_available || is_flow_required);
L88  starting   = continuing && isTimedOut(time_last_fuse, 2e6);
```
L82 的源码注释本身写明设计意图：*"use a relaxed time criteria to enable it to coast through bad range finder data"* —— 它就是被设计成**在地距数据坏掉时也要继续用**。

换算系数来源（确认是 EKF 自己的地距，与传感器自报 `distance_m=0` 无关）：

```cpp
// optflow_fusion.cpp
const float range = predictFlowRange();          // 用于把角速率换算成线速度
float Ekf::predictFlowRange() {
  const float hag = max(_terrain_vpos - _state.pos(2) - pos_offset_earth(2),
                        max(_params.rng_gnd_clearance, 0.01f));
  return hag / _R_to_earth(2,2); }
// EKF2.cpp:1621   lpos.dist_bottom = getTerrainVertPos() - lpos.z;   ← 与 _terrain_vpos 同源
```

加重危害：`resetFlowFusion()` 赋给速度状态的方差是 `calcOptFlowMeasVar()` 的返回值，**量纲是 (rad/s)² 却被当 (m/s)² 使用** → 重置后滤波器以 σ≈0.15 m/s 的置信度完全相信那个 −42.5 m/s。这正是 §3.3 中 `eph≤1.18 m` 的另一半原因。

EKF2 甚至自己算出了合法区间并公开发布，但从不据此自我约束：`t=100.51 s` 同一帧 `vehicle_local_position` 里 `hagl_min=0.20 / hagl_max=5.00 / vxy_max=20.00 / dist_bottom=27.22 m`；`t=224.0~240.5 s` 为 `hagl_max=5.00` 而 `dist_bottom=118.24 m`。（`hagl_min/max` 取有限值仅 39/589 帧，都是光流短暂成为唯一水平源的瞬间；事故瞬间 t=87.1 仍是 `inf`。`distance_sensor` 量程 0.02~12.0 经 `0.75×max` 会给出 `[0.02, 9.0]`，日志中从未出现 → 证明 5.0 确实来自 `SENS_FLOW_MAXHGT` 分支。）

---

## 4. 真实运动还原（独立真值）

`baro_alt_meter` 不可用作物理高度：`estimator_baro_bias.bias` 自 `t=0.2 s` 起恒为 **−24.7 m**（σ≈0.42 m），且 `z_global=0`（EKF 无全局高程基准）⇒ `baro_alt_meter ≈ 原始气压高度 − 24.6 m`，即它的零点对应在起飞点上方约 24.6 m 处。

用 `sensor_baro.pressure` 反算（起飞点 = 0）：

| t (s) | 0 | 10 | 20 | 25 | 40 | 65 | 85 | **87.34** | 92.9 | 110 | 112.5 | 115 | 120~290 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 气压高度 (m) | 0 | 3.0 | 14.8 | 22.7 | 24.3 | 29.0 | 28.6 | **27.9** | **33.4 峰值** | 23.5 | 18.4 | 8.5 | **4.4~5.9（静止）** |
| GPS MSL (m) | 35.05 | 37.9 | 48.3 | 56.8 | 57.1 | 59.0 | 60.8 | 60.6 | — | 57.5 | 52.2 | 39.5 | 41.4（静止） |

- 两条独立曲线在爬升段互差 <1.6 m ⇒ 气压高度刻度可信（且 t≤13 s 与激光逐点吻合：激光 6.27 m @12.92 s vs 气压 6.2 m）。
- 扣除气压热漂移与动压耦合后（温度 52.75→49.4 °C；`气压−GPS` 差从 0 涨到 +2.8 m @t=85、+5.4 m @t=90~95，约 1~1.7 m/°C，15 m/s 时另有 ~5 m 静压耦合），**事故瞬间离起飞点真实高度约 23~25 m**。
- 坠地后静止点高度 `+5 m`（气压）/ MSL 41.4 vs 起飞 35.05 ⇒ **炸机点地面比起飞点高约 4~7 m**。
- ⇒ **飞手"目测最多 20 m"是离地高度(AGL)，成立**；"飘出去约 200 m 炸机"亦成立（GPS：撞地点 214.5 m、最远 254.0 m @t=104.9、飞跑段 87.3~114 s 累计航程 253.9 m、最终静止 ~208.9 m）。
- 高度剖面：**失控瞬间不降反平**（87→100 s 维持 27.9→29.3 m，满推力平飞 ~13 s），第二次 `reset_vel_to_flow`（100.47）之后才开始掉高度，110→115 s 用 5 s 落下 15 m 撞地。**从幻影速度出现到撞地共 26 s，其中约 13 s 是满推力平飞。**

---

## 5. 对旧报告的修正清单

| # | 旧报告表述 | 修正 | 依据 |
|---|---|---|---|
| 1 | "t=87.0 真实角速率突跳 245°/s" 作为拐点，暗示存在物理扰动 | 大角速率发生在 `reset_vel_to_flow` **之后 140 ms**（87.482 s，262.9°/s）；重置前 86.8~87.12 s 的 \|w\| 峰值仅 **37.1°/s**。物理扰动说被排除 | 50 Hz `vehicle_angular_velocity` 原始行；0.5 s 分箱 + 发布时刻偏晚 220 ms 共同造成旧结论 |
| 2 | "EKF 内部 dist_bottom 发散到 **140 m**，该错误地距是 −42 m/s 幻影速度的放大系数" | reset 瞬间 `dist_bottom = 27.86~28.89 m`，**不是 140 m**。139.9 m 出现在 **t=232 s（坠地后 119 s）**，hagl 创新 127.7 m / 测试比 174 亦全在坠地后。放大系数应修正为 **28.9 m（= 光流 5 m 包线的 5.6 倍）** | `estimator_local_position` 原始行 |
| 3 | "气压高度 +6.8→−19.1 m / 全程 −25.2~+8.7 m / 26 m 高差判坠地" | 该列基准被 EKF 抬到起飞点上方 **24.6 m**（`estimator_baro_bias=−24.7 m`、`z_global=0`）。真实物理落差 **≈18 m**，且炸机点地面比起飞点**高** 4~7 m，不是低 19 m | §4 |
| 4 | `EKF2_EV_NOISE_MD = 0` ★不采用 SLAM 自报协方差★，并建议"改为 1 以采用自报协方差" | **方向写反**。v1.15.4 参数原文：*"If set to 0 (default) the measurement noise is taken from the vision message and the EV noise parameters are used as a lower bound. If set to 1 the observation noise is set from the parameters directly."* ⇒ **0 = 采用自报；1 = 忽略自报**。且这条恰恰是 `eph` 虚高的原因之一，按旧建议方向改会走偏 | `params_external_vision.yaml` / `EKF2::UpdateExtVisionSample` |

| 5 | "GPS 完全禁用（`EKF2_GPS_CTRL=0`）导致无绝对定位兜底"，并把 `jamming_state=1 / spoofing_state=1` 读作"常置干扰/欺骗标志" | **枚举读错。** `msg/SensorGps.msg`：`JAMMING_STATE_OK=1`（2=Warning,3=Critical）、`SPOOFING_STATE_NONE=1`（2=INDICATED,3=MULTIPLE）。实测飞行段 `jamming_state≡1`、`spoofing_state≡1` ⇒ **接收器报告"未被干扰、未被欺骗"**。真实情况是 GNSS 基本可用：`fix=3D` 占 99.7%、星数中位 9、hdop 中位 1.66、eph 中位 1.98 m；只是尾部质量差（eph p95=6.35 / p99=8.68、最少 4 星）。**⇒ "用 GNSS 做兜底"从"环境不允许"变成"值得做，但必须配质量门控"**（见 §11.4） | `msg/SensorGps.msg` L33-44 + 本日志 `sensor_gps` |
| 6 | "`NAV_RCL_ACT=3(Terminate)` 被 `CBRK_FLIGHTTERM=121212` 禁用 → 退化为 AUTO_LAND" | **枚举读错，推论随之不成立。** `commander_params.c`：`@value 1 Hold / 2 Return / **3 Land** / 5 Terminate / 6 Disarm` ⇒ **`NAV_RCL_ACT=3` 本就是 Land**，从未配过 Terminate。这与实测 `nav_state` 在 113.54 s / 212.53 s 两次进入 18(AUTO_LAND) **完全自洽**。所以正确表述是：RC 失效保护动作配的是 **AUTO_LAND（需要正确的位置/速度反馈才有效，而二者都是错的）**，而不是"Terminate 被禁用" | `commander_params.c` NAV_RCL_ACT 定义 + 本文 §2 |

另需**撤回本文第一轮的一处误判**（记录以自警）：曾以"EV 测试比自 86.80 s 起爬升 20→422→723→1023→1483"为据判"视觉先坏"。那是把 2 Hz 的创新量按 0.1 s 分箱并对空箱线性插值造出的**假斜坡**。原始行显示：86.745 完全干净，87.240 才是第一异常行，且该行的创新值恰等于幻影速度模长，属**结果**。

---

## 6. 参数修改清单（v1.15.4，不动固件）

> 表内"当前值"全部取自本日志解锁时的 1178 个参数快照，已确认这些参数在你们固件中存在。
> 标 ⚙ 者为 `@reboot_required`，改完须重启。

### 6.1 A 组 · 必做（源码级可证明有效）

| 参数 | 当前 | 改为 | 依据 | 代价 |
|---|---|---|---|---|
| **`EKF2_OF_CTRL`** | 1 | **0** | `optical_flow_control.cpp:84` 的与项；为 0 则既不续融合也永不启动 → **`reset_vel_to_flow` 代码上不可达**。module.yaml 中它本是 boolean、默认 0 | 失去光流备降（该传感器 20 m 以上只产垃圾） |
| `EKF2_TERR_MASK` | 3 | **1** | 位 0=测距、位 1=光流供地形；`_terrain_vpos` 即 `predictFlowRange()` 的乘数，OF 关闭后位 1 应清掉 | 无 |
| **`CBRK_FLIGHTTERM`** | 121212 | **0** | 121212 是"禁用飞行终止"的魔术值。**只要它存在，任何配置成 Terminate 的失效保护都不可达** | 终止=自由落体，须配地面净空纪律。注意：本案里 RC 失效配的是 `NAV_RCL_ACT=3=Land`（非 Terminate，见 §5-6），所以**恢复 CBRK 只是让"将来可以选 Terminate"成为可能**，本身不改变现状行为 |
| `COM_RCL_EXCEPT` | 4 | **0** | `@bit 2 Offboard`。`offboard_control_mode.position` 全程为 1，该例外在切 OFFBOARD 时会关掉 RC 失效保护 | 失去 offboard 期无视 RC 的便利 |
| `SENS_FLOW_MAXR` | 8 | **1.5** | 唯一生效的光流门，且只查未补偿模。实测未补偿峰值 7.948（距 8 仅 0.6%）、**判废 0 次**；肇事样本 1.785 → 1.5 可拦 | 纵深防御，不替代第 1 条；日后重开光流会压缩可用动态范围 |

### 6.2 B 组 · 强烈建议（消除上游诱因）

| 参数 | 当前 | 改为 | 依据 |
|---|---|---|---|
| **`EKF2_RNG_CTRL`** | 2 | **1** | 枚举 `0 禁用 / 1 条件模式 / 2 总是启用`。conditional 原文 = *"range finder used during low speed (<EKF2_RNG_A_VMAX) and low altitude (<EKF2_RNG_A_HMAX) operation, eg takeoff and landing"*。你们已配 `A_HMAX=5 / A_VMAX=0.2`，而激光实测可用 0.04~6.27 m ⇒ **条件模式把它锁回真实量程**，并消除坠地后 `reset_hgt_to_rng/baro/ev` 30 ms 三连抢权 |
| **`EKF2_HGT_REF`** ⚙ | 2（Range sensor） | **0（Barometric）** | 枚举 `0 气压 / 1 GPS / 2 测距 / 3 视觉`。原文警告：*"The range sensor and vision options should only be used when for operation over a flat surface as the local NED origin will move up and down with ground level."* 你们把高度基准设给了量程 6~7 m 的激光；GPS 不可用故不能选 1 |
| `EKF2_MAG_TYPE` ⚙ | 1（Magnetic heading） | **5（None）** | 枚举 `0 Automatic / 1 Magnetic heading / 5 None`。现在同时融磁航向与 EV yaw（`EV_CTRL` bit3），实测 2.5 s 内 yaw 摆 77°、`starting_vision_yaw_fusion` 10~11 次。**偏航源二选一**：或 `MAG_TYPE=5` 只信 SLAM，或 `EKF2_EV_CTRL: 11→3` 只信磁 |
| `EKF2_MULTI_IMU` | 3 | **1**（临时） | `CAL_ACC0/1_ROT=-1`、`CAL_GYRO0/1/2_ROT=-1`（旋转未标定），`CAL_MAG1_PRIO=75>CAL_MAG0_PRIO=50`。主实例切换 6 次（87.37/89.03/97.38/99.44/126.43/184.48），**每次切换伴随一次 vxy/z/heading 全状态重置**（reset 计数递增时刻 89.08/97.37/99.48/126.48/184.47 逐一对应）。补齐 `CAL_*_ROT` 后再恢复 3 |
| **`EKF2_EV_NOISE_MD`** | 0 | **1** | 见 §5-4。同时 `EKF2_EVP_NOISE 0.1→0.3`、`EKF2_EVV_NOISE 0.1→0.2`、`EKF2_EVA_NOISE 0.1→0.15`，让 `eph/evh` 反映真实水平，`COM_POS_FS_EPH`/`COM_VEL_FS_EVH` 才有触发可能。**代价：失效更易被判定 → 阈值必须先在系绳台上按实测 eph/evh 分布标定，否则误触发** |
| **`SDLOG_PROFILE`** ⚙ | 1 | **3**（先只加 bit1） | bit1 = *"Full rate estimator (EKF2) replay topics"*，其中含 **`vehicle_visual_odometry`** 与 **`ekf2_timestamps`（`visual_odometry_timestamp_rel` = EV 输入帧龄）** ⇒ 今天所有关于"丢帧/谁先坏"的代理推断，下次可直接观测。**不要一次加 bit4（高速率，→19）**：本日志已严重自适应降频（gyro/gps 掉到 1 Hz），同时开会挤掉估计器 topic |

### 6.3 C 组 · 兜底与告警（诚实标注本次拦不住的部分）

| 参数 | 当前 → 建议 | 判定 |
|---|---|---|
| `COM_POSCTL_NAVL` | 0 → **1（Land/Descend）** | 原文：位置精度不再够用于位置控制时的响应；选 1 = 认为无人接管，直接 Land/Descend。**按现场是否净空决定** |
| `COM_POS_FS_EPH` / `COM_VEL_FS_EVH` | 5→2 / 1→0.5 | ⚠️ **单改无效**：飞跑段 `eph≤1.18 m`、`evh≤1.53 m/s`，阈值根本不触发。必须与 6.2 的 `EV_NOISE_MD=1` 同时生效 |
| `NAV_RCL_ACT` | 3（**Land**，不是 Terminate）→ **5（Terminate）**，前提是先做 6.1 的 `CBRK_FLIGHTTERM=0` | ⚠️ 本次唯一数学上必然有效的自动动作是 **Terminate（断推力让它原地掉）**：Hold/Land/Return 全部依赖位置与速度反馈，而失控期间二者都是错的。日志实证：两次进入 AUTO_LAND（113.54 / 212.53 s）合计仅 0.7 s 就被踢回，且 AUTO_LAND 期间飞机仍在满推力逃跑 —— **Land 在这种"估计器主导"的失效里等于没有保护** |
| `FD_FAIL_R` / `FD_FAIL_P` | 60 → **不建议改** | ⚠️ 实测失控段真实最大倾角仅 **43.1°**（\|roll\| 35.6°），`tilt>45°` 累计 **0.00 s** ⇒ 45/60 度门限永不触发。要触发只能降到 **30°**（该段 tilt>30° 最长连续 0.48 s > `FD_FAIL_*_TTRI=0.3 s`），但事故前正常飞行最大 tilt 已达 20.3°，仅剩 10° 余量 ⇒ **误触发风险高于收益** |
| `NAV_DLL_ACT` | 0 → 1 或 3 | `telemetry_status[0]` 在 t≈121~240 s 完全 rx=0（地面站坠地后 8~127 s 失联），现在配的是"无动作" |

### 6.4 D 组 · 明确「不要动」

- **`MPC_XY_VEL_MAX` / `MPC_VEL_MANUAL` / `MPC_XY_VEL_ALL`（均=5）：不要为"打得过幻影速度"而调大。** 幻影 33~44 m/s，调到 10 依旧不够，只会让正常飞行更危险。
- **`EKF2_OF_GATE` / `EKF2_EVP_GATE` / `EKF2_EVV_GATE`：调门限对本事故链无效** —— `resetFlowFusion()` 不做创新一致性检验，还会主动清 `reject_optflow_X/Y`（`L152-153`）。门限只管 `fuseOptFlow()`。
- **`EKF2_EV_DELAY`（=40 ms）暂不改**（⚙）。等 `ekf2_timestamps` 落盘后按 `visual_odometry_timestamp_rel` 实测再辨识；官方明确"时间戳与 timesync 正确的话可以为 0"。
- `SENS_FLOW_MAXHGT=5 / MINHGT=0.2` **已正确，不要再动**（它们在 v1.15.4 里本就不参与估计器判定）。
- `EKF2_MIN_RNG=0.1`、`EKF2_RNG_A_HMAX=5`、`EKF2_RNG_A_VMAX=0.2`、`EKF2_GPS_P/V_NOISE`、`EKF2_REQ_*` 均已是合理值，**保持**。

### 6.5 E 组 · 参数解决不了的（勿指望参数）

1. **光流模块在 29 m 输出垃圾却自报 quality 62~175** → 硬件/固件缺陷。除 `EKF2_OF_CTRL=0` 外，更可靠的是让伴飞计算机在超量程时**自行停止发布** `sensor_optical_flow`。
2. **EV 速度通道（`EKF2_EV_CTRL: 11→15`）——真正能让错误速度"可观测、可修复"的一条，但列为第二阶段**。你们已因坐标/帧搞错撞过一次墙，须先按 §7 在台架验证通过。
3. **kill switch 首次拨动 79 s 不生效** → 固件行为（仅新边沿被执行），参数无解。
4. **接收机 failsafe 期间仍 100 Hz 发帧 → PX4 不设 `rc_lost`、只置 `rc_failsafe`，`manual_control_setpoint` 停发 96.7 s** → 属接收机侧（ELRS/CRSF failsafe 行为与通道值），不是 PX4 参数。
5. **`EV_MAX_INTERVAL=200 ms` 无对应参数**（`EKF/common.h` 常量）→ 只能提高 SLAM 发布率（≥20 Hz）或压 p99 帧间隔 <150 ms。

---

## 7. EV 速度输入的帧规范（第二阶段用，源码核对版）

话题与消息：ROS2 → **`/fmu/in/vehicle_visual_odometry`**，`px4_msgs::msg::VehicleOdometry`（`/fmu/in/vehicle_mocap_odometry` 在 yaml 里但 **EKF2 不订阅**；`/fmu/out/vehicle_odometry` 方向相反）。已在默认 `dds_topics.yaml`，无需 custom topics；消息定义须与固件同 release 分支。

| 字段 | 规范 | v1.15.4 源码行为 |
|---|---|---|
| `pose_frame` | 1=NED，2=FRD（世界系朝向任意） | 位置按帧解释，`isAllFinite()` 才写入 |
| `q` | **FRD机体 → 参考系**（body→world），**不要取逆** | `Quatf q_error(_state.quat_nominal * ev_sample.quat.inversed())` |
| `velocity_frame` | 1=NED / 2=FRD(世界) / **3=BODY_FRD** | 见下方三种处理 |
| `velocity` | m/s；**无效必须填 NaN**（填 0 = 合法的"绝对零速度"会被当真融合） | 三通道独立判定有效性 |
| `angular_velocity` | 机体 FRD rad/s | **EKF2 完全不读**（`flowSample`/`extVisionSample` 无该字段） |
| `position_variance`/`velocity_variance` | m²、(m/s)²，报**真实**值 | `NOISE_MD=0` 时 `fmaxf(参数², 自报)`；填 0 ⇒ 被钳到 σ=0.1 m / 0.1 m/s = 近乎无限信任 |
| `reset_counter` | 外部估计器自身重置时递增 | 变化时仅在"该通道是唯一水平辅助源"才 `resetVelocityTo` |

三种 `velocity_frame` 的差异（`ev_vel_control.cpp`）：

| 取值 | 处理 | 依赖 | 适用 |
|---|---|---|---|
| 1 NED | `vel = ev.vel − vel_offset_earth`，**要求 `yaw_align` 已完成** | 需 SLAM 世界 == PX4 本地 NED（原点+朝向都对） | 能保证 map 与本地原点对齐时 |
| 2 FRD(世界任意朝向) | 若已融 EV yaw 直接用；否则 `R_ev_to_ekf · vel`（`R` 来自对 msg `q` 的滑窗旋转误差滤波） | **需 `q` 正确**（机体轴必须 FRD） | 地图朝向任意但姿态转换正确 |
| **3 BODY_FRD（推荐）** | `vel = _R_to_earth · (vel − vel_offset_body)` | 用 **EKF 自己的姿态**，**不碰 msg 的 q**；只需机体轴为 FRD + 杆臂 `EKF2_EV_POS_X/Y/Z` | ★对地图原点/朝向完全免疫 |

REP 147（SuperLIO/ROS 常用：世界 ENU、机体 FLU）→ PX4（世界 NED、机体 FRD）：

```
① 世界系向量(位置/世界系速度):  (X,Y,Z)_ENU → (Y, X, −Z)_NED
② 机体系向量(body twist):       (x,y,z)_FLU → (x, −y, −z)_FRD     ← 只翻 y 与 z
③ 姿态四元数(wxyz, Hamilton):  q_px4 = q(0, 1/√2, 1/√2, 0) ⊗ q_ros ⊗ q(0, 1, 0, 0)
                                 └ ENU世界→NED世界(180°绕(1,1,0)) ┘   └ FLU→FRD机体(180°绕机体x) ┘
```

不开飞的三步自检：① 静置任意朝向 → 在 `velocity_frame=3` 下 `vx/vy` 恒 ≈0；② 手持前推 1 m → `vx`(NED) 为正、`vy`≈0（若 `vy` 大而 `vx` 小 ⇒ 混进 90°/−90° 旋转，典型为 ENU↔NED 未换 x/y）；③ 抬升 1 m → `vz` **必须为负**（为正 ⇒ z 未翻）。

EV 速度被真正融合的全部条件（缺一不可）：`EV_CTRL` bit2 ∧ `tilt_align` ∧ velocity 全有限 ∧ frame ∈{1,2,3} ∧ 样本 ≥5 Hz 且时间线"最新" ∧ **启动一致性检验：若已有其它水平辅助在跑，要求 `test_ratio < 0.1` 才允许接管** ∧ 方差三层下限 ∧ 创新门 `EKF2_EVV_GATE=3`。
⇒ **幻影速度存在时 EV 速度根本不会启动**（`test_ratio ≫ 0.1`）。所以"开 bit2"必须常态化在线才有效，不是出事后才打开的救火开关。

---

## 8. 改完必须做的验证（不做等于没改）

A+B 组改完 → **重启** → 台架/系绳 5 分钟 → 拉日志跑断言：

```
1) estimator_event_flags.reset_vel_to_flow   全程 0 次     ← 核心目标
2) estimator_status_flags.cs_opt_flow        全程恒 0
3) cs_ev_pos 在线占比 >99%，vision_data_stopped 0 次
4) cs_rng_hgt 仅在 HAGL<5 m 时为 1                          （验证 EKF2_RNG_CTRL=1）
5) -z 与 sensor_baro.pressure 反算高度一致                   （验证 EKF2_HGT_REF=0）
6) eph/evh 分布明显变宽                                      （验证 EV_NOISE_MD=1 生效）
7) estimator_selector_status.primary_instance 切换 0 次      （验证 MULTI_IMU=1）
8) 人为制造 ≥1 s EV 断流：期望 cs_inertial_dead_reckoning 置位、
   xy_valid→0、执行 COM_POSCTL_NAVL 指定动作，
   且【速度状态不被任何脏数据改写】                          ← 真正的验收试验
```

---

## 9. 复现资料

分析脚本（本目录，依赖 `pip install pyulog`，用法 `python <脚本> 05_46_38.ulg [t0 t1 step]`）：

| 脚本 | 用途 |
|---|---|
| `ulog_baro_vision.py` | baro 本体/EKF 融合质量、EV 创新、在线占比、真值网格 |
| `ulog_bv2.py` | baro 硬指标、`ev_pos_bias`、`dist_bottom` 时间线、坠地窗口真值 |
| `ulog_bv3.py` | 拐点 0.1 s 精排（**注意：分箱插值会造假斜坡，只可用于 ≥5 Hz 的话题**） |
| `ulog_bv4.py` | 真值极值、baro/GPS 热漂移、分阶段在线占比、主实例切换 |
| `ulog_who_is_wrong.py` | **原始行零插值**：创新/测试比/事件/光流/IMU 逐行对照 |
| `ulog_slam_vs_gps.py` | 创新反解 SLAM 测量值 vs GPS；光流物理不可能样本清单 |
| `ulog_mag_test.py` | 免坐标系对齐的模长判据（速度模/位移模） |
| `ulog_why_stick_fails.py` | 在线占比、位置/速度重置计数对比、杆量-设定值-饱和对照、双时间基 |
| `ulog_vision_dropout.py` | 丢帧专项：`cs_ev_*` 离线条块、事件分阶段计数、timesync/telemetry 探针 |
| `ulog_flow_gates.py` | `SENS_FLOW_MAXR` 门限余量与判废次数反查、`hagl_min/max` 时间线 |
| `ulog_dump_params.py` / `ulog_param_extra.py` | 参数快照导出、SDLOG/CAL_*/姿态极值 |

源码依据（v1.15.4，已下载至 `%TEMP%\px4src\PX4-Autopilot-1.15.4\` 做全文检索）：
`src/modules/ekf2/EKF/optical_flow_control.cpp`（L49-56 / L82-89 / L120-137 / L139-156）、`EKF/optflow_fusion.cpp`（L51-87 / L163-177）、`EKF/ekf_helper.cpp`（`get_ekf_ctrl_limits` L286-331）、`EKF/common.h`（`flowSample` L212-217、`EV_MAX_INTERVAL`）、`EKF2.cpp`（`UpdateFlowSample` L2323-2388、`L1621`、`L1640`、`L2518`、`L1091`）、`src/modules/sensors/vehicle_optical_flow/VehicleOpticalFlow.cpp:243-272`、`src/modules/sensors/sensor_params_flow.c:56-97`、`src/modules/ekf2/module.yaml`、`src/modules/commander/commander_params.c`、`src/modules/logger/params.c:112-147`、`msg/VehicleOdometry.msg`、`msg/EstimatorEventFlags.msg`、`msg/TimesyncStatus.msg`。

**v1.15.4 之后官方已补的防护**（main 分支，已确认 v1.16.0 包含；**v1.15.x 未发现任何 backport**）：
`9d6c2baa90` *ekf2-flow: only allow flow when in range*（补上 `is_within_sensor_dist = getHagl() ∈ [_flow_min_distance, _flow_max_distance]`，其上限正是 `SENS_FLOW_MAXHGT`）；`a5a67315fd` *magnitude check compensated*（改为也检查补偿后速率，且不合格不得用于 reset）；`6c24413888` *filter flow vel (used for flow velocity reset)*；`9183c479a5` *correctly compute vel variance from flow variance*（修 (rad/s)²→(m/s)² 量纲 bug）；`13c5bd4412` *do not reset position when resetting to flow*。
⇒ **本案所需的配置你们本来就已配对（`SENS_FLOW_MAXHGT=5`），只是 v1.15.4 尚未实现它。** 若允许定向 backport，上述 5 个 commit 是约 6~30 行的改动，覆盖本案每一步。

---

## 10. 未证实 / 待现场确认

1. `telemetry_status[1]`（5760、`mavlink_v2=0`、rx ≈7400 msg/s、`rx_packet_drop_count` 全程 +1705）**对应哪条物理串口**、是否与 uXRCE-DDS 共用 → 决定"+1705 持续丢包"是否与里程计相关。
2. EV 速度反解依赖 ECL 的 `innov = pred − obs` 约定（已用"哪个符号约定能复现 GPS"做了实证：飞跑段 −innov 列 0.99 m/s vs +innov 列 57.57 m/s），**但原始 `vehicle_odometry` 未落盘，无法一锤定音**。加 `SDLOG_PROFILE` bit1 后可直接验证。
3. SuperLIO 的 odom 发布率、`reset_counter` 行为、是否上报真实协方差、时间戳取的是采集时刻还是发布时刻 —— 均需回 ROS2 侧确认。
4. 高度 20 m（飞手目测，AGL）与 27.9 m（气压，离起飞点）之间 ~5~8 m 的差异，由"气压热/动压偏差 3~5 m"+"炸机点地面高出起飞点 4~7 m"共同解释；因激光在 7 m 以上无输出，**7 m 以上不存在独立的离地高度(AGL)真值**。
5. `EKF2_EV_VEL_LIM` / `EKF2_EV_WGT` / `EKF2_REL_*` 在 v1.15.4 **不存在**（已核对参数绑定与 module.yaml），勿再按网上旧资料查找。

---

## 11. 同类隐患排查（第二轮，专项扫描）

### 11.1 方法

单次事故的正确交付物不是"修好这一个传感器"，而是**把事故抽象成可复用的结构模式，再用每个模式回扫全系统**。本案可拆成 7 个模式：

| 模式 | 定义 | 本案是否命中 |
|---|---|---|
| **P1** | 传感器超出自身量程/包线后仍在被采信 | ✅ 光流（29 m vs 5 m 包线）、激光测距（7 m 归零 vs 声明 12 m） |
| **P2** | 单帧测量可以**重置**状态而不经过一致性检验 | ✅ `reset_vel_to_flow` |
| **P3** | "用重置冒充融合"会喂饱所有基于数据新鲜度的看门狗 | ✅ **本轮新证，危害极大** |
| **P4** | 自报协方差为 0 ⇒ 无限信任 ⇒ 所有基于 eph/evh 的保护被静默旁路 | ✅ |
| **P5** | 名义冗余 ≠ 真实冗余 | ✅ **本轮新证** |
| **P6** | 失效动作指向本机不可达的模式 | ✅ **本轮新证** |
| **P7** | 输入通路"看似活着、实则无效" | ✅ RC（本案）+ **offboard/数传 本轮新证** |

扫描脚本：[ulog_hazard_scan.py](file:///d:/projects/robot/drone/px4-log/0911/ulog_hazard_scan.py)、[ulog_hazard2.py](file:///d:/projects/robot/drone/px4-log/0911/ulog_hazard2.py)、[ulog_hazard3.py](file:///d:/projects/robot/drone/px4-log/0911/ulog_hazard3.py)

### 11.2 命中清单（按严重度排序）

| # | 模式 | 隐患 | 证据（实测） | 处置 |
|---|---|---|---|---|
| H1 | **P3** | **陈旧性看门狗被"重置"喂饱 → 所有依赖它的保护集体失效** | 失控持续 3 分钟，`stopping_navigation`、`height_sensor_timeout`、`gps_fusion_timout`、`emergency_yaw_reset_*` **全程 0 次**；`EKF2_NOAID_TOUT=5 s` 从未生效；`xy_valid` 直到 **227.9 s** 才变 0（失控后 **141 s**）。机理：EV 位置每秒被 `resetHorizontalPositionTo` 一次，reset 同时刷新 `time_last_fuse` ⇒ "最后一次成功融合"永远新鲜 | 这是**结构性**问题，参数无解。唯一出路：让错误可被观测（见 H2、H5），并把 Terminate 作为兜底（§6.3） |
| H2 | **P4** | `eph/evh` 虚高 ⇒ 位置/速度失效保护永不触发 | `COM_POS_FS_EPH=5 m`、`COM_VEL_FS_EVH=1 m/s`、`COM_ARM_EKF_POS=0.5/VEL=1/HGT=1/YAW=0.5`；而飞跑段 `eph≤1.18 m`、`evh≤1.53 m/s`。根因：`EKF2_EV_NOISE_MD=0` + SLAM 自报方差为 0 ⇒ `fmaxf(0.1², 0)` ⇒ 告诉 EKF "位置准到 10 cm" | §6.2 `EV_NOISE_MD=1` + 实测化 `EVP/EVV/EVA_NOISE` |
| H3 | **P6** | **两个失效动作配成 Return，但本机无全局位置 → 不可达** | `global_position_invalid` 自 t=0 起**全程置位**（`EKF2_GPS_CTRL=0`）；`COM_QC_ACT=0` = 枚举里的 **Return mode**；`COM_FLTT_LOW_ACT=3` = **Return**。（`COM_LOW_BAT_ACT=2`=Land 是对的） | 改 `COM_QC_ACT=1`(Land) 或 `2`(Hold)；`COM_FLTT_LOW_ACT=0/1`（无全局位置时飞行时间估计本就无意义） |
| H4 | **P5** | **三 IMU 冗余是假的** | `CAL_ACC0_ID == CAL_ACC2_ID == 4259850`（同一颗加速度计登记为两个索引）⇒ 3 个 gyro 但只有 2 颗 accel；`sensor_selection` 两实例分别用 (accel 2490378 + gyro 2490378) 与 (accel 4259850 + gyro 4325386) 的**不同配对**；而 `CAL_ACC0/1_ROT`、`CAL_GYRO0/1/2_ROT`、`CAL_MAG0/2/3_ROT` **全为 −1（旋转未标定）**，只有 `CAL_MAG1_ROT=0`，且 `CAL_MAG1_PRIO=75 > CAL_MAG0_PRIO=50` | §6.2 `EKF2_MULTI_IMU=1`（临时）+ 补齐 `CAL_*_ROT`。⚠️ **注意 6 次切换全在 87.37 s 之后 ⇒ 切换是失控的后果，不是原因**；但一旦失控，它会把"旋转未标定的两套基准"互换，放大发散 |
| H5 | **P7** | **伴飞计算机的 offboard 位置流全程挂着，撞地后仍推了 114 s** | `offboard_control_mode.position` 在 **0.1~294.8 s 恒为 1**；`failsafe_flags.offboard_control_signal_lost` **直到 t=227.81 s 才第一次出现**；`COM_OF_LOSS_T=60 s`（阈值极长）、`COM_OBL_RC_ACT=4`=Land。当前 `nav_state` 全程非 OFFBOARD（POSCTL 累计 228.1 s），所以没被采纳 | 见 §11.5 N1（代码侧）+ `COM_OF_LOSS_T 60→2`、`COM_RCL_EXCEPT=0` |
| H6 | **P1** | 激光测距通道**完全没有自证能力**，却被设为**高度基准** | `distance_sensor.signal_quality ≡ −1`（不提供质量位）、`max_distance` 声明 12 m（实测可用 0.04~6.27 m）、`EKF2_RNG_CTRL=2`（总是启用）、`EKF2_HGT_REF=2`（Range sensor）。后果：`cs_rng_hgt` 全程切换 **19 次**，坠地后 `reset_hgt_to_rng/baro/ev` 在 30 ms 内三连触发 | §6.2 `EKF2_RNG_CTRL=1` + `EKF2_HGT_REF=0` |
| H7 | **P1** | 气压计：单颗、有热漂移与动压耦合，且无第二路交叉校验 | 仅 1 个 `device_id=4028193`；温度 52.75→44.02 °C；`气压高度 − GPS高度` 差从 0 涨到 +2.8 m（t=85）、+5.4 m（t=90~95，恰为 14~15 m/s 高速段）⇒ 约 **1~1.7 m/°C** 热致偏差 + 约 5 m 动压耦合。本案里它恰好是**唯一正确的高度源**，但这是运气 | 无参数可解。列为已知误差；落地时用 `MPC_ALT_MODE`/限速段规避；若硬件有第二路 baro 可开 `EKF2_...` 需先确认本固件支持（**未证实**） |
| H8 | **P2** | 同一条"重置"通路还有多个兄弟入口 | `EstimatorEventFlags` 里同类位：`reset_vel_to_flow`（本案）、`reset_vel_to_gps`、`reset_vel_to_zero`、`reset_pos_to_last_known`、`reset_pos_to_gps/vision`、`reset_hgt_to_baro/gps/rng/ev`。本案日志实际出现的：`reset_hgt_to_baro` 3 次、`reset_hgt_to_rng` 2 次、`reset_hgt_to_ev` 2 次（全在坠地后 117~222 s） | 逐项消除其数据源即已覆盖：OF 关（H-§6.1）、RNG 条件化（H6）、GNSS 若开必须配 `EKF2_GPS_CHECK`（见 11.4） |
| H9 | — | 悬停推力假设与 EKF 估计值差 **29%** | `MPC_THR_HOVER=0.5`，而 `hover_thrust_estimate.hover_thrust` 中位 **0.644**（`hover_thrust_var≈0`，即 EKF 很确信）。失控段 `thrustZ=−1.00` 连续 25 s ⇒ 向上余量被系统性高估 | 二选一：`MPC_THR_HOVER` 设 0.64，或确认 `MPC_THR_HOVER` 是否被设为"自动估计"语义（**v1.15.4 未证实有自动选项，需核**）。改后 failure detector 的电机裕度判据基准随之变化 |
| H10 | — | 自整定可在现场被自动应用，当前增益疑似已来自整定 | `MC_AT_EN=1`、`MC_AT_APPLY=1`（`MC_AT_START=0`）；`MC_ROLLRATE_P=0.165404`、`MC_PITCH_P=5.65852`、`MC_ROLL_P=5.77331` 等带整定尾数。无任何整定记录随参数留存 | `MC_AT_APPLY=0`（防止现场静默改增益）；整定结果入配置库并留档 |
| H11 | — | 姿态速率上限极高，与"光流角速率包线"完全不匹配 | `MC_ROLLRATE_MAX = MC_PITCHRATE_MAX = 533 °/s`（`MC_YAWRATE_MAX=100`）；本案失控 140 ms 内即达到 **262.9 °/s**，光流未补偿角速率随即冲到 1.7 rad/s | 可选：降到 250~300 °/s，直接压低光流/SLAM 的毛刺幅度。⚠️ 代价：机动性与抗扰能力下降，属权衡项而非必做 |
| H12 | — | 三道物理/流程闸门被魔术值长期关闭 | `CBRK_IO_SAFETY=22027`（忽略安全开关，实测 `safety_off≡1` 全程）、`CBRK_USB_CHK=197848`（忽略 USB 锁定检查）、**`COM_ARM_HFLT_CHK=0`**（默认 1 = 启用；关掉后 **SD 卡上有 hardfault 记录也照样解锁**） | 至少恢复 `COM_ARM_HFLT_CHK=1`（这是免费的故障探针）。另两项按你们的台架/外场流程取舍 |
| H13 | — | 失控后飞控一直判定"此刻不允许解锁"，但电机仍被驱动 175 s | `pre_flight_checks_pass` 在 113.5 s 后长期为 0（全程占比 1 仅 0.13）；同期 PWM 仍 109/1999、电流 25~55 A | 说明**告警数据源是现成的，只是没人接**。把 `vehicle_status.pre_flight_checks_pass / failsafe / failure_detector_status` 与 `estimator_event_flags` 一起接入地面站声音告警 |

### 11.3 本轮新增的参数条目（接在 §6.1 A 组之后执行）

| 参数 | 当前 → 建议 | 理由（对应隐患） |
|---|---|---|
| `COM_QC_ACT` | 0(Return) → **1(Land)** | H3：无全局位置时 Return 不可达 |
| `COM_FLTT_LOW_ACT` | 3(Return) → **0 或 1(Warning)** | H3 同上；且无 GNSS 时"到达返航点所需时间"的估计本身不成立 |
| `COM_ACT_FAIL_ACT` | 0（Warning only）→ 至少 **2(Land)** | `FD_ACT_EN=1` 已启用执行器失效检测，但动作被设成只告警 |
| `COM_OF_LOSS_T` | 60 s → **2 s** | H5：60 s 的 offboard 失联容忍对一条常挂的幽灵流毫无意义 |
| `COM_ARM_HFLT_CHK` | 0 → **1** | H12：免费的 hardfault 探针 |
| `MC_AT_APPLY` | 1 → **0** | H10：禁止现场静默改增益 |
| `MPC_THR_HOVER` | 0.5 → **0.64**（需台架确认语义） | H9 |
| `MPC_XY_VEL_MAX` 等限幅 | **不动** | 见 §6.4 |
| `MC_ROLLRATE_MAX` / `MC_PITCHRATE_MAX` | 533 → **300**（可选，权衡项） | H11 |

### 11.4 GNSS 兜底的量化可行性（因 §5-5 更正而重新评估）

GNSS 实际是**可用但尾部质量差**：`fix=3D` 99.7%、星数中位 9、hdop 中位 1.66、eph 中位 1.98 m；但 eph p90=5.17 / p95=6.35 / p99=8.68，最少 4 星。

用现成的 294 s 数据直接算各套门限的通过率：

| 门限组合 | 通过率 |
|---|---|
| **现配** `REQ_EPH≤3, REQ_PDOP≤2.5, REQ_NSATS≥6, REQ_EPV≤5, REQ_SACC≤0.5` | **22.4%** ← 会频繁启停 |
| 放宽 `EPH≤5, PDOP≤5, NSATS≥8, EPV≤8, SACC≤1.0` | **79.4%** |
| 仅 `NSATS≥8 且 EPH≤5` | 83.7% |

建议（若要开）—— **⚠️ 本节结论已被 §12 修订，以下仅为历史记录**：
- `EKF2_GPS_CTRL = 0 → **1**`（**只开 bit0 水平位置，不要开 bit2 速度**——理由见 §12.4；原写 5 是错的）
- **必须同时** `EKF2_GPS_CHECK = 0 → 31`（bit0 星数 / bit1 PDOP / bit2 EPH / bit3 EPV / bit4 SACC；bit5/6 的 HDRIFT/VDRIFT 需先在地面静止段实测再决定，故先取 31 而非 127）
- `EKF2_REQ_EPH 3→5`、`EKF2_REQ_PDOP 2.5→5`、`EKF2_REQ_NSATS 6→8`、`EKF2_REQ_EPV 5→8`、`EKF2_REQ_SACC 0.5→1.0`
- **额外收益**：GNSS 水平辅助一旦活跃，`startFlowFusion()` 就走 `fuseOptFlow()` 分支而**不可能**再 `resetFlowFusion()` —— 等于给 §6.1 第 1 条之上再加一道独立防线
- ⚠️ **不能替代 §6.1 第 1 条**；且开 GNSS 会引入新变量：EKF 本地原点将由 `xy_global/z_global` 变为全局、GNSS 与 SLAM 之间的固定偏置会互相拉扯（受 `EKF2_GPS_P_NOISE=0.5` / `EKF2_EVP_NOISE` 支配）。**必须先做 §8 的台架验证，并在同一地点比对 SLAM 轨迹与 GNSS 轨迹的固定偏移与朝向**，确认无系统性偏差后才可带位置控制飞行。

### 11.5 参数解决不了、必须做工程动作的（N1~N5）

| # | 动作 | 对应隐患 |
|---|---|---|
| N1 | 伴飞计算机：**不在真正使用 OFFBOARD 时不得置位 `offboard_control_mode`**；同时给 odometry 加"发布看门狗"（SLAM 节点自身异常时停发而非续发陈旧值） | H5、P7 |
| N2 | 伴飞计算机：光流模块在测距超量程（>5 m）时**停止发布** `sensor_optical_flow` —— 比飞控侧任何门限都可靠，因为是断源 | H1/P1 |
| N3 | 接收机：核对 ELRS/CRSF 的 failsafe 行为，使射频断链时**停止输出帧**（让 PX4 能置 `rc_lost`），而不是保持 100 Hz 发送冻结通道值 | 本案 96.74 s 无杆量输入 |
| N4 | 建立"失控可观测"闭环：把 `estimator_event_flags`、`vehicle_status.failsafe/pre_flight_checks_pass/failure_detector_status`、`eph/evh` 接入地面站**声音**告警，并把 `vision_data_stopped` 定义为**立即降落**的硬触发（59.048 s 那次"未遂"若被告警，本次事故可提前 28 s 避免） | H1、H2、H13 |
| N5 | 现场核实：`telemetry_status[1]`（5760、`mavlink_v2=0`、rx≈7400 msg/s、`rx_packet_drop_count` 全程 +1705）对应哪条物理串口、是否与 uXRCE-DDS 共用；以及 SuperLIO 的 odom 发布率、`reset_counter` 行为、是否上报真实协方差、时间戳取采集时刻还是发布时刻 | §10-1/3 |

### 11.6 本轮新发现但**未证实**的（不下结论，仅登记）

1. `cs_gnd_effect` 置 1 占比 **0.82**，而 `EKF2_GND_MAX_HGT=1.5 m`、`EKF2_MCOEF=0.15`、`EKF2_ARSP_THR=0`（无空速）。地面效应模型在 28 m 高度长期呈"启用"状态，语义与是否有实际影响我未能从 v1.15.4 源码逐行确认 —— 列为待查，不作为隐患结论。
2. `hover_thrust_estimate.hover_thrust` 最小值出现 **0.30**（中位 0.644），且 `accel_innov` 范围 −99.5~+45.0、`accel_innov_var` 最大 6.5e5 —— 疑似起飞初值/收敛过程，未定位具体成因。
3. `MC_ROLLRATE_MAX=533 °/s` 是否来自某次自整定，日志无整定事件记录可证。
4. `failsafe` 位在 `vehicle_status` 中占比 0.72，但 `failsafe_flags` 的变化点显示首个真实失效（`manual_control_signal_lost`）出现在 113.53 s —— 两者口径不一致的原因（锁存语义 vs 边沿语义）未逐行核实，不影响本案结论。

---

## 12. GNSS 专项与参考系朝向问题（第三轮，含对本文 §11.4 的修订）

> 新增样本：`03_05_59.ulg`（2026-09-11 03:05:59，587 s，仓库内正式作业飞行，配置与事故 log 完全一致）。
> 场地经纬度 40.31187/122.14303，与事故点 40.28018/122.15635 相距 ~3.5 km，同一日早 2h41m。

### 12.1 结论摘要（本轮）

| # | 结论 | 置信度 |
|---|---|---|
| G1 | 仓库 log 里 **GNSS 全程 0 星、0 fix**，但**接收器前端/通信/时钟全部正常** | 高（实测） |
| G2 | 事故 log 里 GNSS **自身极稳、零跳变**；"信号问题导致定位跳变"不成立 | 高（实测） |
| G3 | **PX4 的水平参考系（含机首向）相对物理世界一致旋转约 +50°** | 高（三源互证），但根因归属未定 |
| G4 | 该旋转在你们的闭环内**不可见**（setpoint↔状态自洽，实测恒等） | 高（实测） |
| G5 | "开 GNSS 会瞬移 ~58 m"这个担忧被源码证伪（`collect_gps` 有重投影保护） | 高（源码） |
| G6 | **残余真实风险**：`resetVelocityToGnss`（速度硬置）与 `ev_yaw_control.cpp:63-66`（GPS 上线会踢掉 EV yaw） | 高（源码） |
| G7 | 你们固件是 **fork**，含 8 个官方不存在的 EKF2 参数，其中 `EKF2_ECEF_ROT_*` 已填值但功能关闭 | 高（检索），语义未证实 |

### 12.2 两份 log 的 GNSS 实测

**仓库 log（03_05_59，587 帧，全程）**

| 字段 | 取值 | 判读 |
|---|---|---|
| `fix_type` / `satellites_used` / `vel_ned_valid` | ≡ 0 / ≡ 0 / ≡ 0 | 从未定位，且**源码级静默**（见 12.4-①） |
| `hdop` | ≡ **100.0** | u-blox 无效哨兵值 |
| `eph` / `epv` | 145 424~306 360 m / 118 859~229 957 m | 由无效 hdop 派生 |
| `altitude_msl_m` | ≡ **21330.15 m** | 无效哨兵值 |
| `latitude/longitude_deg` | 常数，std=0 | 从未更新 |
| **`time_utc_usec`** | 587/587 有效、**相邻精确推进 1.000 s、0 次倒退**（03:06:00.500 → 03:15:46.500） | **接收器在正常输出 NAV-PVT，串口/链路/时钟全好** |
| **`noise_per_ms`** | **84~89**（正常 L1 C/A 量级） | **射频前端在工作**；天线断路/短路时噪声底通常异常 |
| `jamming_indicator` | 38~178（在变） | 干扰检测在跑，峰值偏高 |
| `jamming_state` / `spoofing_state` | 0=UNKNOWN / 1=NONE | 与"从未定位"一致；事故 log 是 1=OK / 1=NONE |
| `automatic_gain_control` / `rtcm_injection_rate` | ≡ 0 / ≡ 0 | AGC 未实现，不可作判据 |

分阶段：**最前 60 s（室外起飞段）星数 max = 0**、60~120 s = 0、**末 120 s（含飞出与降落）星数 max = 0**。

判读（需现场核对，见 12.5-V3）：
- 开头段 **可能是**搜星时间不够（冷启动需 25~35 s，若上电后很快进仓则永远拿不到第一帧）；
- 结尾段 **不像**搜星时间问题：接收器在室内通常仍保留星历/历书，出到开阔地是热启动（典型 1~10 s），而末 120 s 全 0 星；
- 同一架机 2h41m 后在另一场地 13 星 / hdop 1.28 / eph 1.0 m ⇒ 当天晚些时候硬件可用（**前提：两份 log 是同一架机，请核对**）；
- ⇒ 排查方向：天线馈线/SMA 接头、天线安装位遮挡（金属顶棚/碳纤维/大电流线束）、接收机 ITFM 干扰抑制配置（`jamming_indicator` 峰值 178）。

**事故 log（0911，取 vision 可信窗 0~86.8 s）**

| 指标 | 实测 |
|---|---|
| fix / 星数 / hdop / eph / s_acc | 3D 100% ／ 10~13 ／ 1.28~1.65 ／ 0.97~1.12 m ／ 0.20~0.47 m/s |
| **跳变度量**（相邻历元"vision−GNSS 偏移向量"的变化） | 中位 0.36 m、p95 2.80 m、**max 3.44 m**、>5 m **0 次**、>10 m **0 次** |
| 速度一致性（模长） | 全体历元差中位 **+0.04 m/s**；运动段 \|差\| 中位 **0.14 m/s**、p95 0.73；静止假速度 p95 0.62 m/s |
| 位移尺度比（GNSS vs vision） | **1.026**（差 2.6%） |
| 垂直（去常值） | 中位 0.68 m、p95 2.84、max 3.12（epv p50=1.50）；气压同法 0.48/1.47/2.79 |
| GNSS 冻结事件 | **0 段** |
| 门限通过率（本窗） | 现配 `REQ_*` 通过 100%、`EKF2_GPS_CHECK=0` 100%、严格(EPH≤2/PDOP≤1.5/NSATS≥10/EPV≤3/SACC≤0.3) 67.4%、极严 0% |

⇒ **GNSS 是这两份日志里最干净的传感器之一。真正拦在 GNSS 纳入前面的不是信号质量，是 12.3 的朝向问题。**

### 12.3 参考系朝向：~50° 旋转的三个独立测量

| 检验 | 结果 | n |
|---|---|---|
| A. GNSS 地迹方向 − `vehicle_local_position` 位移方向 | 圆均值 **+46.8°**，逐历元 +38°~+53°（失控后历元离群），位移模仅差 2.6% | 47 |
| B. **GNSS 地迹方位 − `vehicle_attitude` 机首向**（直线、倾角<12°、hdop<5、>2 m/s） | **圆均值 +45.9°、中位 +51.0°、MAD 7.6°** | 21 |
| C. 倾斜补偿磁方位 − EKF 机首向（水平姿态<15°，起飞后 0-60 s 平稳段） | 中位 **−53.2°、MAD 2.4°** | 118 |

对齐方式对照（同一 0~113 s 窗）：恒等旋转下残差 0-20/20-40/40-60/60-80 s = 17.6/9.6/8.8/30.6 m；**去除 +46.8° 旋转后 = 2.46/2.32/1.25/2.04 m**（降到 GNSS 自报精度量级）。
旋转特征检验：恒等旋转下残差 vs 离原点距离 相关 **+0.910**、斜率 **1.080 m/m** ⇒ 典型固定旋转特征，排除随机误差/尺度错/位置漂移。
垂直方向去常值残差仅 **0.68 m** ⇒ **绕天轴的纯旋转能同时解释"水平差 50° 而垂直吻合"**。

**自洽性验证（否证"两套北"）**：仓库 log 中 659 个有限 `trajectory_setpoint.position` 对 `vehicle_local_position` 回归：
```
 sp_x = +0.9988·lx + 0.0001·ly + 0.05   RMS 0.152 m
 sp_y = −0.0005·lx + 1.0007·ly + 0.02   RMS 0.166 m
⇒ 等效旋转 −0.02°、尺度 0.9997；|sp−lpos| 中位 0.18 m、max 0.67 m，且不随距离增长
 （窗口内离原点最远 111.4 m；若 setpoint 与状态真差 50°，误差应为 2·111·sin25° ≈ 94 m → 排除裕度 ~140×）
```
速度侧独立判据：`arg(sp_vel)−arg(v_actual)` 中位 **+0.4°**、MAD 4.7°、模长比 0.983。事故 log 同法 −0.53°。
⇒ **你们的 ROS↔PX4 实时对齐是正确的，setpoint 与状态同帧；本文前一版把"恒等"解读为"对齐未生效"是错的。**

**⇒ 综合判断（根因归属未定，见 12.5-V1/V2）**：
> PX4 输出的水平参考系（姿态 yaw + 位置）**一致地**偏离物理世界约 50°。它在闭环内部不可见（所以飞行、建图、到点全部正常），但任何绕过对齐、直接使用"物理北"或"PX4 帧"的东西都会中招：GNSS 融合、罗盘、lat/lon 航点与任务、home/RTL、地理围栏、地面站地图叠加。

旁证（该锁定不牢固）：`heading_reset_counter` 在 89.08/97.37/99.48 s 递增、`starting_vision_yaw_fusion` 全程 10~11 次、曾观测到 2.5 s 内 yaw 摆 77°。

### 12.4 odom ↔ yaw 闭环机理猜想（交源码侧排查）

**猜想一句话**：`vehicle_visual_odometry` 的姿态四元数如果是"以 PX4 自身输出为基准"反算出来的，则 EKF 的 yaw 观测来自 EKF 自己的 yaw，绝对航向在闭环里不可观，被初始条件钉死（实测钉在 +50°），并可在 odom `reset_counter` 变化时被硬置。

**ECL 侧的确切接触点（v1.15.4 原文）**：

① `ev_yaw_control.cpp:48-50` —— **你送进来的四元数的 yaw 就是 yaw 观测**：
```cpp
aid_src.observation = getEulerYaw(ev_sample.quat);
aid_src.observation_variance = max(ev_sample.orientation_var(2), _params.ev_att_noise, sq(0.01f));
aid_src.innovation = wrap_pi(getEulerYaw(_R_to_earth) - aid_src.observation);
```
⇒ `q` 必须是 **body→earth**，且其中 earth 必须是**真北 NED**。若构造 `q` 时用到了 `/fmu/out/vehicle_odometry`、`/fmu/out/vehicle_attitude`、或以 PX4 的北为基准的 TF，就形成回路。

② `ev_yaw_control.cpp:80-86` —— **odom 自身 reset 时整帧 yaw 被硬置**（这就是 snap 的入口）：
```cpp
if (ev_reset) {                      // 由 VehicleOdometry.reset_counter 变化触发
    if (quality_sufficient) {
        ECL_INFO("reset to %s", "EV yaw");
        resetQuatStateYaw(aid_src.observation, aid_src.observation_variance);   // ★硬置 yaw，非渐进
```
⇒ 若你们的对齐在任意条件下递增 `reset_counter`（或 SuperLIO 自身重定位），EKF 机首向会**瞬间跳到 odom 四元数的 yaw**。实测的 77°/2.5 s 与本案中的 yaw 反复 `starting_vision_yaw_fusion` 与此一致。

③ `ev_yaw_control.cpp:62-71` —— **这条决定了"开 GNSS"的真实风险，务必注意**：
```cpp
// if GPS enabled don't allow EV yaw if EV isn't NED
if (_control_status.flags.gps && _control_status.flags.yaw_align
    && (ev_sample.pos_frame != PositionFrame::LOCAL_FRAME_NED)) {
    continuing_conditions_passing = false;         // EV yaw 被强制停用
```
⇒ **一旦 GNSS 开始融合，而你们发布的 `pose_frame` 是 `2 (FRD/arbitrary heading)`，EV yaw 会被立刻停掉，yaw 归属源切到磁**（你们 `EKF2_MAG_TYPE=1`）——而在 `CAL_MAG*_ROT=−1`（7 个旋转未标定）的条件下，**这就是一次 yaw 大跳**。这条与"GNSS 位置跳不跳"无关，是**另一个**开关，前一版分析完全遗漏。
⇒ 反之若你们发布 `pose_frame=1 (NED)`，EV yaw 不会被停，但那等于**向 PX4 断言"我的 odom 世界系就是真北"**——若实际旋转 50°，这正是把整个参考系钉偏 50° 的那一步。
**所以 `pose_frame` 的取值必须在朝向问题解决后才能定，且它直接改变 yaw 的归属源。**

④ 相关参数/状态位（供取证时对照，均已核对 v1.15.4 绑定）：`EKF2_EV_CTRL` bit3(YAW)、`EKF2_MAG_TYPE=1`、`EKF2_HDG_GATE=2.6`（用于磁航向与 GNSS yaw，**不用于 EV yaw**）、**`EKF2_EVA_NOISE=0.1`**（`EKF2.cpp:176 → _params.ev_att_noise`，是 EV 姿态/yaw 观测方差的**下限**，0.1 rad ⇒ σ≈5.7°，**非常小 ⇒ EV yaw 权重极高、几乎必然主导航向**）、`estimator_status_flags.cs_ev_yaw / cs_mag_hdg / reject_mag_hdg`、`heading_reset_counter`、`estimator_innovations.mag_hdg / ev_yaw`。
⚠️ **未检索到 EV yaw 专用的创新门参数**（`common.h` 只有 `ev_pos_innov_gate`/`ev_vel_innov_gate`）⇒ **EV yaw 的一致性检验门限在 v1.15.4 无法单独收紧**，能动的只有 `EVA_NOISE`（放大下限）与 `EV_CTRL` 位。这对"用参数防住 yaw 被 odom 硬拽"是一个实质限制。

**三条判定实验**：
- **E1（2 分钟，最优先）**：机头精确对准已知地理方位（真北标记，或用 GNSS 双点定向拉 20 m 直线定方位），静置水平，上电解锁不飞 → 读 `vehicle_attitude` yaw 与地面站罗盘。差<10° ⇒ 50° 只在位置链路；差≈50° ⇒ **飞控认为的机头方向本身就是错的**，先修磁/朝向源。
- **E2**：把桥接代码里 `vehicle_visual_odometry.q` 与 `pose_frame` 的**构造式**打出来，核对是否引用了任何 `/fmu/out/*` 或 PX4-origin 的 TF。若引用 ⇒ 闭环成立，改为"odom 用 SLAM 自己的 world（`pose_frame=2`+显式 `q`）"或直接对齐到真北。
- **E3**：记录同一时刻的三件事——SLAM 地图的 X 轴物理方位、GNSS 地迹方位、`vehicle_attitude` yaw。重复 5 次上电。**若每次 SLAM/PX4 相对真北的偏角都不同 ⇒ 朝向是上电随机的，任何"一次性标定"都不成立，必须运行时估计。**

### 12.5 对本文前面章节的更正

| # | 原文（本文前一版） | 更正 | 依据 |
|---|---|---|---|
| 7 | §11.4 及本轮初稿：*"开 GNSS 位置融合会在启动瞬间把位置硬重置，实测瞬移包络 0~58.7 m"* | **证伪（有条件）**。`gps_checks.cpp:63-77` 的 `collect_gps()` 在原点首次初始化时，若 `isHorizontalAidingActive()`（EV 位置在融合）会**把当前 EKF 位置反投影成经纬度作为原点** ⇒ 两套坐标被强制在当前位置重合，**瞬移量 ≈ 0** | `collect_gps()` L72-77 |
| 8 | §11.4：建议 `EKF2_GPS_CTRL = 0 → 5`（水平位置 + 3D 速度） | **改为 → 1（只开水平位置，不开速度位）**。因为 `gps_control.cpp:157-165` 的启动条件里 `isTimedOut(_time_last_hor_vel_fuse, 7 s)` 在"没有任何东西在融合速度"的配置下**恒为真** ⇒ 开 bit2 就必然在 GNSS 启动/重获那刻执行 `resetVelocityToGnss`（把 GNSS 速度硬写进状态）。门口/刚捕获帧的多普勒速度最不可信 | 源码 + 你们配置（EV 速度位关闭、GNSS 关、光流即将关） |
| 9 | 本轮初稿：*"对齐结果恒等 ⇒ 对齐可能形同虚设"* | **撤回**。以机身实时为基准的双向闭合，恒等是**正确且预期**的结果；该检验只能证明"setpoint 与状态同帧" | §12.3 回归 |
| 10 | 本轮初稿：*"50° 旋转解释了上次开 EV 速度融合撞墙"* | **撤回该归因**。setpoint 与状态同帧 ⇒ 50° 在内部闭环不可见，不会造成"到不了点"。上次撞墙最可能仍是 `velocity_frame`/轴序声明错误。**无法用现有两份日志证实** | 同上 |
| 11 | §11.4 前提：*"GNSS 可用（据 0911），值得纳入兜底"* | **补限定**：正式仓库场景实测 GNSS 全程 0 星（§12.2），该前提在作业场地不成立 | 实测 |

### 12.6 GNSS 纳入检查清单（修订版，按顺序，缺一不可）

| # | 前置 | 现状 |
|---|---|---|
| P0 | 确认作业场地 GNSS 硬件可用（先解决 0 星问题），并确认两份 log 是否同机 | ❌ |
| P1 | **接收机导航速率 1 Hz → ≥5 Hz**（两份 log dt 中位均 1000 ms；`GNSS_MAX_INTERVAL=500 ms`，1 Hz 下 `_gps_intermittent` 恒真） | ❌ |
| P2 | **解决 ~50° 朝向**（E1 判定 → E2/E3 定位 → 修 `CAL_MAG*_ROT` 等） | ❌ |
| P3 | 定 `pose_frame`：必须先知道 P2 的结论，并注意 `ev_yaw_control.cpp:63-66` 的 GPS↔EV-yaw 互斥 | ❌ |
| P4 | `EKF2_GPS_CHECK = 0 → 31`（位定义：`MASK_GPS_NSATS`=1/PDOP=2/HACC=4/VACC=8/SACC=16，`gps_checks.cpp:51-59`）；`REQ_*` 按 12.2 通过率表定值 | ❌ |
| P5 | `EKF2_GPS_CTRL = 0 → **1**`（只位 0；**不要 bit1 高度、不要 bit2 速度**） | ❌ |
| P6 | `EKF2_HGT_REF = 2 → 0`（与 §6.2 同）；GNSS 不接管高度 | ❌ |
| P7 | diff 你们的 fork 源码 vs 官方 v1.15.4，确认 `gps_control.cpp` / `ev_yaw_control.cpp` / `collect_gps` 未被改动 | ❌ |
| P8 | 台架/系绳按 §8 断言验证，并额外盯 `estimator_event_flags.reset_pos_to_gps / reset_vel_to_gps / 任何 yaw reset` 必须为 0 | ❌ |

**注**：完成 P2 之前，**不要**开 `EKF2_GPS_CTRL`。而 `EKF2_OF_CTRL=0` 与保护链（`CBRK_FLIGHTTERM=0`、`NAV_RCL_ACT=5`、`COM_QC_ACT`、`COM_RCL_EXCEPT=0`）与本节无关，优先级仍更高、可立即执行。

### 12.7 fork 告警（影响本文所有源码结论的适用范围）

参数快照里有 **8 个官方 v1.15.4 全树检索 0 命中** 的 EKF2 参数：
```
EKF2_ECE_CR_FILT=0.10  EKF2_ECEF_ROT_X=+21.44°  EKF2_ECEF_ROT_Y=−4.57°  EKF2_ECEF_ROT_Z=−34.86°
EKF2_EKFI_EPH_TEST=1.0  EKF2_EKF_LAT_LON=0  EKF2_EKF_VEC_VEL=0  EKF2_ENERGY_EST=1  EKF2_EN_EASDZ=15
```
从命名看极可能是"**让 EKF 接收外部 ECEF / 经纬度原点 / 大地速度，并定义 ECEF→local 旋转**"的自定义功能，且 **`ECEF_ROT_*` 已被人填过一组欧拉角**（Z=−34.86°，与实测 ~50° 不一致，可疑），而 `EKF_LAT_LON=0`、`EKF_VEC_VEL=0` 表明当前关闭。
⇒ **若它的语义确是"在 EKF 内部定义地理北"，那你们需要的真北对齐接口已经在固件里，且比在 setpoint 侧加旋转干净得多。这是本轮最有价值的待确认项。**
⚠️ 限定：凡涉及这些非官方参数所触及的代码路径，本文核对的官方源码不保证等价。已验证且与日志逐项吻合的路径（`optical_flow_control.cpp` 启/续条件、`VehicleOpticalFlow.cpp:243-272` 参数转发、`gps_control.cpp` 启动重置、`collect_gps` 重投影、`ev_yaw_control.cpp:48/85/63-66`）结论仍成立，但请按 P7 做一次 diff 兜底。

### 12.8 本轮脚本

[ulog_gnss_vs_vision.py](file:///d:/projects/robot/drone/px4-log/ulog_gnss_vs_vision.py)（平移对齐、跳变度量、门限仿真）、[ulog_gnss_v2.py](file:///d:/projects/robot/drone/px4-log/ulog_gnss_v2.py)（速度一致性、任意时刻锁定原点的偏差推演）、[ulog_gnss_rot.py](file:///d:/projects/robot/drone/px4-log/ulog_gnss_rot.py)（旋转 vs 漂移判别）、[ulog_yaw_vs_mag.py](file:///d:/projects/robot/drone/px4-log/ulog_yaw_vs_mag.py)（倾斜补偿磁方位）、[ulog_yaw_vs_cog.py](file:///d:/projects/robot/drone/px4-log/ulog_yaw_vs_cog.py)（**50° 在姿态里还是位置里的判决**）、[ulog_gps_health.py](file:///d:/projects/robot/drone/px4-log/ulog_gps_health.py)（搜星时间 vs 天线/硬件判据）、[ulog_frame_consistency.py](file:///d:/projects/robot/drone/px4-log/ulog_frame_consistency.py)（两套北的恒等性检验）。

---

## 13. 第四轮：本文(§12.4)偏航结论的实测修正 + 代码侧现状清单

### 13.1 撤回三条（都是我这轮之内或上一轮说的，逐条列明）

| # | 我曾说 | 实测/复核结果 | 性质 |
|---|---|---|---|
| a | 提出并反复引用 `map_heading_offset` 这个"参数/字段" | **全库 0 命中**：本文 0、txt 报告 0、`xtd2_ws` 代码与配置 0、1178 个参数快照无此项。**是我在对话中造的名字** | 凭空杜撰，作废 |
| b | `body_to_ned_3d_matrix` 非正交、ψ→45°/135° 退化、"定时炸弹" | 代码**正确**。`Rz(ψ)·diag(1,-1,-1)` = `[[c,s,0],[s,-c,0],[0,0,-1]]`，实测任意 ψ：**det=+1.0000、M·Mᵀ=I**，恒为正交旋转。C++ 实现(`coordinate_transform.cpp:162-170`)与 Python 逐元素一致 | 我信了函数自己的**错误注释**(`coordinate_transform.py:259-261, 284`)而未验算 ⇒ 误判 |
| c | `CAL_MAG1_ROT=0=UNDEFINED` 是危险信号 | `SENS_MAG_AUTOROT = 1` ⇒ 外置罗盘朝向由运行时自动判定，`ROT=0` 是**预期值**，不是缺陷 | 过度解读 |

⇒ 另需撤回本文 §11.7(6) 中"该矩阵非正交、需单独立项"一条：**不立案，只改注释**。

### 13.2 对本文 §12.4-④ 的实测修正：航向的话语权不是 EV yaw 的

§12.4-④ 写「`EKF2_EVA_NOISE=0.1` ⇒ σ≈5.7°，**EV yaw 权重极高、几乎必然主导航向**，磁只是名义上启用」。本日志参数快照否证其后半句：

```
EKF2_MAG_E_NOISE   = 0.001 rad = 0.057°   [官方默认 0.5236 rad = 30°]  ← 被改小了 523 倍
EKF2_MAG_B_NOISE   = 1e-4                  [官方默认 0.3]
EKF2_EVA_NOISE     = 0.1   rad = 5.73°     （默认值，未改）
EKF2_HDG_GATE      = 2.6   （默认）
EKF2_MAG_CHECK     = 1     （只查强度，不查航向一致性）
EKF2_MAG_DECL      = -4.4529°（该参数确实存在；本机真磁偏约 -7°，差 2~3°）
⇒ 权重比 (磁 : EV) = (5.73/0.057)² ≈ 10 000 : 1
⇒ 磁独占航向；且 EV yaw 的容差被压到 2.6×0.057° = 0.15°
```

实测印证（事故前 30~86.8 s 稳态段）：
```
estimator_innovations.heading   中位 +0.0024°  std 0.51°  max 2.75°   ← 航向跟踪极好
cs_yaw_align      100%          cs_ev_yaw_fault   恒 0
cs_ev_yaw         97.5% 在线，且 t=-0.96s（解锁前）即置 1
```

**但这不意味着闭环无害 —— 危害路径要换一条。** §12.4-② 的 `resetQuatStateYaw()` 是**硬置状态**，不经过加权、不经过一致性检验：

> ⇒ 在 `EKF2_MAG_E_NOISE=0.001` 下，**融合路径上 odom 拽不动航向；但 reset 路径可以一次性把航向硬置成 odom 给的任意值，且事后没有任何创新量会报警**。
> 本文 §12.3 旁证列出的 `heading_reset_counter` 在 89.08/97.37/99.48 s 递增，应优先按"reset 硬置"复核，而不是按"渐进被拽偏"复核。这两者的修法不同：前者只需切断 `ev_reset` 的来源（不再递增 reset_counter / 关 bit3），后者才需要调噪声与门限。

### 13.3 `ev_yaw` 创新量不可用作验收判据（本文与旧报告都曾这样用）

桥接现状 `multirotor_communication.py:849`：`px4_msg.q = self.cur_vehicle_odometry.q`（回填 PX4 自身输出）。
⇒ `aid_src.innovation = wrap_pi(eulerYaw(R_to_earth) − eulerYaw(ev_quat))` 的两项**同源**，创新恒 ≈0 是**恒等式**。

> **因此 §12.4 用"EV yaw 创新很小"来支持/否定任何结论，都不成立；而 13.2 里那个"稳态航向跟踪极好"同样不能用它来判定 map 朝向是否正确。**
> map 朝向对不对，只能靠带外基准（本文 E1 / GNSS 双点定向），不能靠创新量。
> ⇒ 已把这条写进 §13.6 的自警。

### 13.4 设计定案（据"北由 GPS 天线内的磁提供"）

| 项 | 原建议 | **改为** | 理由 |
|---|---|---|---|
| `EKF2_MAG_TYPE` | §6.2：1→5(None) | **保持 1**（若要完整倾角+磁偏补偿则 →4；注意 v1.15.4 枚举无 7） | 北由磁给，**绝不能关磁** |
| `EKF2_EV_CTRL` | 待拍板（11→3 或 11→15） | **11→3**，且提到波次 0 | 既然 SLAM 不提供真北，就不该把 SLAM 姿态灌进 EKF 当偏航观测。关 bit3 同时切断 §13.2 的硬置路径 |
| 罗盘工程动作 | 补 `CAL_MAG*_ROT` | **改为：重做罗盘标定 + 用 E1 判定 ~50° 是否来自磁** | 13.1-c：ROT=0 在 AUTOROT=1 下是正常的，不该拿它开刀 |
| `EKF2_MAG_E_NOISE` | — | **挂起**，先查是谁要求从 0.5236 改成 0.001 | 若当初为压某个抖动源，回退会放出那个问题 |

⚠ 与 §12.6-P3 的衔接：`EKF2_EV_CTRL` 改为 3 之后，§12.4-③ 那条"GPS 上线会踢掉 EV yaw"的互斥**自动失效**（bit3 已关），P3 里关于 `pose_frame` 会改变 yaw 归属的顾虑也随之降级；但 **`pose_frame` 仍决定位置通道的解释方式**，P2/P3 顺序不变。

### 13.5 参数波次总表（v4 定稿，合并 §6 / §11.3 / §12.6 / §13.4）

```
波次0 零风险，可立即执行（与朝向问题无关）
  EKF2_OF_CTRL     1 → 0     reset_vel_to_flow 代码上不可达   ← 直接死因
  EKF2_TERR_MASK   3 → 1
  EKF2_EV_CTRL    11 → 3  ★本轮上调★ 关 EV 偏航融合（§13.2/§13.4）
  COM_RCL_EXCEPT   4 → 0  |  COM_QC_ACT 0 → 1  |  COM_FLTT_LOW_ACT 3 → 1
  COM_ACT_FAIL_ACT 0 → 2  |  COM_ARM_HFLT_CHK 0 → 1  |  MC_AT_APPLY 1 → 0
  EKF2_MAG_TYPE    保持 1（撤销 §6.2 的 1→5）

波次1 需 reboot + 台架/系绳验证
  EKF2_HGT_REF 2 → 0 ⚙   |  EKF2_RNG_CTRL 2 → 1   |  EKF2_MULTI_IMU 3 → 1
  SDLOG_PROFILE 1 → 3 ⚙（落 vehicle_visual_odometry + ekf2_timestamps，
                            并补 §12.4 需要的 aid_src 逐源 innovation/reject_flags）
  COM_OF_LOSS_T 60 → 2（依赖 13.6-C1 完成）

波次2 有前置依赖，不可提前
  MD 路线二选一定案后，才动 COM_POS_FS_EPH 5→2 / COM_VEL_FS_EVH 1→0.5
  NAV_RCL_ACT 3(Land)→5(Terminate) 前提 CBRK_FLIGHTTERM=0 —— 需按现场净空拍板
  EKF2_GPS_CTRL：先做完 §12.6 的 P0~P2（尤其 P2 朝向），否则不开

波次3 权衡/可选
  SENS_FLOW_MAXR 8→1.5 | MPC_THR_HOVER 0.5→0.64 | MC_ROLL/PITCHRATE_MAX 533→300
  挂起：EKF2_MAG_E_NOISE 0.001→? | EKF2_MAG_B_NOISE 1e-4→? | EKF2_DECL_TYPE 3→7
```

### 13.6 代码侧现状（逐项实机核验）

**已完成（未提交）**
| 项 | 证据 |
|---|---|
| 删墙钟限频 | `odom_callback_min_interval` 全文件 0 命中；改判 `odom_max_latency`（3 处）。**附带结案 §10-3 / §E组-5**：`/lio/robo/odom` 实为 IMU 率驱动（`ROSWrapper.cpp:669 imuHandler → :747 push → :1057 imuOutputThread → :1128 publish`），10 Hz 瓶颈在被删的限频，不在 LIO |
| 跳变守卫 | `L923-928` 用 **NaN 关闭 position/velocity 通道**，不整帧丢弃（丢弃会造空洞 → `vision_data_stopped` → `reset_vel_to_flow`，即本次事故路径） |
| reset_counter 链路 | `L240-242` 订阅 `/lio/odom_reset_counter`；`L671-690` 处理 LIO 重启回退；`L834` 跳变侧递增；`L943` `&0xFF` 发往飞控 |
| 协方差上报 | Super-LIO `fillOdometryCovariance`，速度块做 `R^T·P_vv·R`；`infl = need_converge ? 4.0 : 1.0`（已从 100 降为 4） |
| LIO 队列溢出 | 由静默 pop 改为计数+限频告警。更正：队列**本已有界**（`ROSWrapper.cpp:711 if(size>10) pop()`），缺陷是"静默"而非"无界" |
| 异常处理 | `except` 改为计数 + 限频告警，不再只打一行日志 |

**待办（剩下这些，按性价比）**
| # | 位置 | 动作 | 性质 |
|---|---|---|---|
| C1 | `multirotor_communication.py:341-349` | `OFFBOARD_STATE=="DISABLED"` 仍发 `position=True`（注释自陈为规避 `no offboard signal`）→ 不发布。**H5 幽灵流的根因就在这里**（实测 `offboard_control_mode.position` 0.1~294.8 s 恒 1） | 真隐患，优先 |
| C2 | `:285-301` | 急刹分支发 `velocity=True`+零速，缺 `nav_state==OFFBOARD` 前置 → 飞控估计跳变时会反复误触发接管 | 真隐患 |
| C3 | Super-LIO `ROSWrapper.cpp:1012-1015` | 三个 `*_FLOOR` 仍为 1e-4/1e-3/1e-4（**未改**，本文上一版描述有误）；若 MD=0 路线成立需抬到 §12 量级 | 依赖 MD 定案 |
| C4 | `coordinate_transform.py:259-261, 284` | **只改错误注释**（13.1-b） | 无功能影响，防误"修" |
| C5 | `multirotor_communication.py:849` | 加注释：bit3=0 后此通道不参与解算；**若重开 bit3 或改 `pose_frame=2`，必须先换成 SLAM 自身姿态**（否则 §13.2 的硬置路径复活） | 门禁，非必改 |
| C6 | `:703-706` | `timestamp` 填 ROS epoch µs vs 规范要求开机 µs | **待核实**（`listener vehicle_visual_odometry`），核实前不动 |
| C7 | `:933` | `px4_msg.quality=100` 仍硬编码 | `EKF2_EV_QMIN=0` 时无效，保持 |

**不改**：`MPC_*` 限幅、`EKF2_*_GATE`、`SENS_FLOW_MAXHGT/MINHGT`、`q` 本体（按 13.4，靠关 bit3 使其成为死通道，比改代码更稳）。

**审计问题（非改动）**：任务/航点坐标究竟在 map 系还是 GNSS 系生成。若混用，§12.3 的 ~50° 会在 `trajectory_setpoint` 上表现为系统性旋转 —— 与 13.1-b 的矩阵无关，是契约问题。

### 13.7 自警（本轮新增，防重犯）

1. **凡方案中出现的标识符，必须标注"现有"还是"待建"**。（`map_heading_offset` 教训）
2. **注释/文档不是证据**。矩阵那一条，两行 numpy 就能验，我当时没验。
3. **同源通道的创新量不能当验证判据**（§13.3）；这条对 §12.4 的全部"创新量很小/很大"论证同样适用。
4. **reset 类代码路径绕过所有门限与加权**：本案 `reset_vel_to_flow`、§12.4-② `resetQuatStateYaw`、§12.5-8 `resetVelocityToGnss` 三处同构。**审 EKF2 时应优先审"哪些路径能硬置状态"，再看噪声与门限。**
5. 编辑本文档时一次误用空 `old_str` 造成全文覆盖（已恢复）。**对整篇文档级修改，禁止用 SearchReplace 的"锚定空串"写法。**

---

## 14. 第五轮：撤销三条代码改动 + 两条归因更正

### 14.1 撤销 C1 / C2 / C3（现场判断优先，理由见下）

| # | 原提案 | 撤销理由 | 我原来的错 |
|---|---|---|---|
| **C1** | `OFFBOARD_STATE=="DISABLED"` 不再发布 `offboard_control_mode` | 心跳常发是为了让模式切换可即时发生（合法需求）；且 `nav_state ≠ OFFBOARD` 时该流对控制**是惰性的**（offboard 失效只在真进 OFFBOARD 时判定） | 把 §11.2-H5 照单全收。**H5 的真实观测不是 `.position=1`，而是"撞机后 `trajectory_setpoint` 仍推了 114 s、`offboard_control_signal_lost` 到 227.81 s 才首次出现"**。前者无害，后者才是问题，两者被我混成一条 |
| **C2** | 急刹分支加 `nav_state==OFFBOARD` 前置 | POSCTL 下急刹分支发什么都不影响飞机；真在 OFFBOARD 里，跳变触发零速也不是坏动作 | "误触发"在两个分支上都不成立 |
| **C3** | 抬高 Super-LIO 的三个 `*_FLOOR` | v1.15.4 在 `MD=0` 下取 `fmaxf(EKF2_EVP_NOISE², 自报值)`，**参数是下限**。自报 1e-4 m²（1 cm）< `EVP_NOISE=0.1 m` ⇒ **飞控自己就钳到 10 cm，我们的地板值在该路径上完全不参与结果** | 提了一个改了也不改变任何行为的改动 |

**遗留待你拍板（不属 C1，且我不擅自改）**：是否要在 `land_detector` / crash / `armed=0` 时**主动停发 `trajectory_setpoint`**（心跳照发，只停 setpoint）。若判断"飞控已 disarm，setpoint 无害"，则记为已知行为、不改。

### 14.2 两条连带更正

**(a) §11.2-H2 的根因表述要改。** H2 写"自报方差为 0 ⇒ 告诉 EKF 位置准到 10 cm"。但 10 cm 是**飞控参数自身的下限**，不是我们报 0 造成的。⇒ `eph≤1.18 m` 的虚高**不能归因于自报为 0**，真正原因是本文自己的 §11.2-H1（`resetHorizontalPositionTo` 每秒刷新新鲜度）+ §3.5（`resetFlowFusion` 的 (rad/s)²→(m/s)² 量纲 bug）。
修法不变（调 `EVP/EVV/EVA_NOISE`），但归因若不改，下次会去查错地方。

**(b) §13.4 的"MD 路线二选一"决定取消。** MD=0 + 低地板的实际语义正是所需：**不会比 `EVP_NOISE` 更信（有 10 cm 下限钳位），而 ESKF 的 P 真变大时（退化场景）能原样传进去**。MD=1 会把这个唯一有益性质扔掉。
⇒ **`EKF2_EV_NOISE_MD` 保持 0，`*_FLOOR` 不改**；要让 `eph/evh` 在退化时明显变大，手段是调 §15 波次 2 的噪声参数，不是动代码。

### 14.3 C4 已完成并回归验证（唯一实际改动：注释）

`coordinate_transform.py:259-261` 与 `:284` 两处**错误注释已更正**，矩阵本身一字未动。

```
回归验证（8 个 ψ 角，含 45°/135°/210°/359°）：
  正交 M·Mᵀ=I : True(全部)   det=+1 : True(全部)   inv(M)==ned_to_body_3d_matrix : True(全部)
  另证：M 对称(M==Mᵀ) 且对合(M²=I) ⇒ 逆矩阵在数值上等于自身
  flu_to_ned_position(1,2,3, ψ=37°) = [2.0023, -0.9955, -3.0]
  py_compile OK；git diff = +11/-4，全部位于 docstring 内
⇒ 函数行为零变化，新注释的三条断言均可复算。
```

⚠ **顺带一个新发现（需你确认是否在意）**：运行时打印
`Warning: C++ implementation not available, falling back to Python implementation`
⇒ 本机 `USE_CPP_IMPL` 实际为 **False**，`src/coordinate_transform.cpp` 那份实现**当前没被加载**。所以 §13.6 里"Python/C++ 逐元素一致"只是**源码层面**的结论，不代表两条路径都在跑；也意味着 `body_to_ned_3d_matrix` 等函数走的一直是 Python 分支。若那个 pybind 模块本来打算启用，值得单独查一次为何没装上。

---

## 15. PX4 参数调整执行清单（最终版，可直接照做）

> 全部"当前值"取自本日志解锁时的 1178 个参数快照，已逐项核对存在性。
> ⚙ = `@reboot_required`，改完必须重启。所有改动**不动固件、不改代码**。

### 15.1 波次 0 · 零风险，可立即执行（与朝向/GNSS 问题无关）

| 参数 | 当前 | → 目标 | 依据 | 风险 |
|---|---|---|---|---|
| **`EKF2_OF_CTRL`** | 1 | **0** | 本文 §6.1：`optical_flow_control.cpp:84` 的与项。为 0 则既不续融合也永不启动 ⇒ **`reset_vel_to_flow` 代码上不可达**（本次直接死因） | 失去光流备降（该模块 20 m 以上只产垃圾，见 §3.5） |
| `EKF2_TERR_MASK` | 3 | **1** | OF 关闭后位 1（光流供地形）应清掉；`_terrain_vpos` 正是 `predictFlowRange()` 的乘数 | 无 |
| **`EKF2_EV_CTRL`** | 11 | **3** | §13.4/§13.2：已定"北由磁提供"，就不该把 SLAM 姿态灌进 EKF 当偏航观测。**且关 bit3 同时切断 §12.4-② `resetQuatStateYaw` 的硬置路径** | SLAM 偏航不再参与解算（本就是既定设计） |
| `COM_RCL_EXCEPT` | 4 | **0** | bit2=Offboard。`offboard_control_mode.position` 全程为 1，该例外在进 OFFBOARD 时会关掉 RC 失效保护 | 失去 offboard 期无视 RC 的便利 |
| `COM_QC_ACT` | 0 | **1**(Land) | §11.2-H3：`global_position_invalid` 全程置位（`EKF2_GPS_CTRL=0`）⇒ 配成 Return **不可达** | 无 |
| `COM_FLTT_LOW_ACT` | 3 | **0** 或 1 | 同上；且无 GNSS 时"到达返航点所需时间"的估计本身不成立 | 无 |
| `COM_ACT_FAIL_ACT` | 0 | **2**(Land) | `FD_ACT_EN=1` 已启用执行器失效检测，但动作被设成只告警 | 无 |
| `COM_ARM_HFLT_CHK` | 0 | **1** | §11.2-H12：免费的 hardfault 探针（关掉后 SD 卡有 hardfault 也照样解锁） | 若 SD 卡有历史 hardfault 记录，将**挡住解锁**（这是目的，但要知道） |
| `MC_AT_APPLY` | 1 | **0** | §11.2-H10：禁止现场静默应用自整定增益（`MC_AT_EN=1`+`MC_AT_START=0` 仍可后台跑） | 自整定结果不再自动生效，需人工确认 |

**做完这一波即可消除本次的直接死因。** 其余波次都是"降低发生概率 / 让错误可见"。

### 15.2 波次 1 · 需重启 + 台架/系绳验证

| 参数 | 当前 | → 目标 | 依据 |
|---|---|---|---|
| **`EKF2_HGT_REF`** ⚙ | 2(Range sensor) | **0**(Barometric) | §11.2-H6：把**整条高度基准**挂在实测可用 0.04~6.27 m 的激光上，是比 OF 更深一层的误配。GPS 不可用故不能选 1 |
| `EKF2_RNG_CTRL` | 2(总是启用) | **1**(条件模式) | §6.2：条件模式 = 低速(<`RNG_A_VMAX=0.2`) 低高(<`RNG_A_HMAX=5`) 才用 ⇒ **把激光锁回它的真实量程**，并消除坠地后 `reset_hgt_to_rng/baro/ev` 30 ms 三连抢权 |
| `EKF2_MAG_TYPE` | 1 | **保持 1**（撤销本文 §6.2 的 1→5 建议；若要完整倾角+磁偏补偿则 →**4**，注意 v1.15.4 **无枚举 7**） | §13.4：北由磁给 ⇒ 绝不能关磁 |
| `EKF2_MULTI_IMU` | 3 | **1**（临时） | §11.2-H4：`CAL_ACC0_ID==CAL_ACC2_ID==4259850` ⇒ 三 IMU 冗余是假的；且 `CAL_*_ROT` 多数未标定。6 次主实例切换**全在 87.37 s 之后**，是失控的**后果**，但一旦失控会把两套未对齐基准互换、放大发散。补齐 `CAL_*_ROT` 后可恢复 3 |
| **`SDLOG_PROFILE`** ⚙ | 1 | **3**（只加 bit1） | §6.2：落盘 `vehicle_visual_odometry` + `ekf2_timestamps`，并补齐 §13.3 需要的 `estimator_aid_src_*` 逐源 innovation/reject_flags ⇒ 本次"谁先坏/30°~50° 在哪侧"之所以判不了，就是因为这些没落盘。**不要一次加 bit4**（本日志已严重自适应降频，gyro/gps 掉到 1 Hz） |
| `COM_OF_LOSS_T` | 60 s | **2 s** | §11.2-H5：60 s 的 offboard 失联容忍对一条常挂的流毫无意义。⚠ 与 §14.1-C1 的遗留问题相关：**setpoint 流全程不停，此项单独改意义有限**，建议与遗留问题一并决定 |

### 15.3 波次 2 · 有硬前置，不可提前

| 参数 | 当前 | → 目标 | 前置条件 |
|---|---|---|---|
| `EKF2_EV_NOISE_MD` | 0 | **保持 0**（撤销 §6.2 的 →1） | 无。§14.2(b)：MD=0 + 下限钳位才是想要的语义 |
| `EKF2_EVP_NOISE` | 0.1 m | **0.3** | 让 `eph` 反映真实水平，`COM_POS_FS_EPH` 才有触发可能 |
| `EKF2_EVV_NOISE` | 0.1 m/s | **0.2** | 同上（`evh`） |
| `EKF2_EVA_NOISE` | 0.1 rad | **0.15** | ⚠ 抬高它同时会放宽 §12.4-① 的 EV yaw 权重下限；**但波次 0 已关 bit3，此项影响面已缩小** |
| `COM_POS_FS_EPH` | 5 m | **2** | **必须晚于** 上面三个噪声参数，否则飞跑段 `eph≤1.18 m` 根本不触发（§11.2-H2） |
| `COM_VEL_FS_EVH` | 1 m/s | **0.5** | 同上。⚠ §14.2(a)：`eph` 虚高的成因是 H1+§3.5，不是自报为 0，别去查协方差上报 |
| `COM_POSCTL_NAVL` | 0 | **1**(Land/Descend) | 按现场净空决定 |
| `CBRK_FLIGHTTERM` | 121212 | **0** | 前提纪律：终止=自由落体，须有地面净空。§5-6：本案 RC 失效配的本来就是 Land，**此项不改变现状行为**，只是让"将来可选 Terminate"成为可能 |
| `NAV_RCL_ACT` | 3(**Land**) | **5**(Terminate) | **需你拍板**。§6.3：本案唯一数学上必然有效的自动动作是 Terminate（Land/Hold/RTL 全依赖正确的位置与速度反馈，而失控期间二者都错；实测两次 AUTO_LAND 合计 0.7 s 就被 `COM_RC_OVERRIDE` 踢回） |
| `NAV_DLL_ACT` | 0 | **1** 或 3 | §11.2-H13 相关：`telemetry_status[0]` 在 121~240 s 完全 rx=0，现在配的是"无动作" |
| `EKF2_GPS_CTRL` | 0 | **暂不改** | §12.6：须先做完 P0(场地可用)→P1(导航速率≥5 Hz)→**P2(~50°朝向)**，且开启时配 `EKF2_GPS_CHECK=31` + 放宽 `REQ_*`。**只开 bit0，不要 bit1/bit2**（§12.5-8） |

### 15.4 波次 3 · 权衡/可选

| 参数 | 当前 | → 目标 | 备注 |
|---|---|---|---|
| `SENS_FLOW_MAXR` | 8 | **1.5** | §6.1：唯一生效的光流门且只查未补偿模（未补偿峰值 7.948，距 8 仅 0.6%、**判废 0 次**）。属纵深防御；**若波次 0 已 `OF_CTRL=0` 则当前无意义**，可作为"将来重开光流"的预置 |
| `MPC_THR_HOVER` | 0.5 | **0.64** | §11.2-H9：与 EKF 实测悬停推力（中位 0.644）差 29% ⇒ 向上余量被系统性高估。⚠ 需台架确认语义（是否"自动估计"，v1.15.4 未证实） |
| `MC_ROLLRATE_MAX` / `MC_PITCHRATE_MAX` | 533 | **300**（可选） | §11.2-H11：直接压低光流/SLAM 毛刺幅度。代价是机动性与抗扰能力，属权衡项 |
| `EKF2_MAG_E_NOISE` | **0.001** rad（默认 0.5236） | **挂起** | §13.4：先查是谁、为什么改成 0.057°。它现在让 EKF 以伪精度无条件相信罗盘，并把 EV yaw 容差压到 0.15° |
| `EKF2_MAG_B_NOISE` | 1e-4（默认 0.3） | **挂起**（同上） | — |
| `EKF2_DECL_TYPE` | 3（默认 7） | 待查后一并评估 | §13.2 实测 `EKF2_MAG_DECL=-4.45°` vs 该点真磁偏约 −7°，差 2~3° |
| `EKF2_EV_DELAY` | 40 ms | **不动** | ⚙ §6.4：等 `ekf2_timestamps` 落盘后按 `visual_odometry_timestamp_rel` 实测再辨识 |
| `SENS_FLOW_MAXHGT/MINHGT` | 5 / 0.2 | **不动（已正确）** | §3.5：v1.15.4 里它们**不参与估计器判定**，改也无用 |
| `FD_FAIL_R/P` | 60° | **不动** | §6.3：实测失控段真实最大倾角仅 43.1°、`tilt>45°` 累计 0.00 s ⇒ 45/60 永不触发；降到 30° 又只剩 10° 余量，误触发风险高于收益 |
| `MPC_XY_VEL_MAX` 等限幅 | 5 | **不动** | §6.4：幻影 33~44 m/s，调到 10 依旧不够，只会让正常飞行更危险 |
| 各 `*_GATE` | — | **不动** | §6.4：`resetFlowFusion()` 不做一致性检验还主动清 reject 标志，门限只管 `fuseOptFlow()` |

### 15.5 执行后必须跑的断言（合并 §8 与本轮新增）

```
 1) estimator_event_flags.reset_vel_to_flow   全程 0 次          ← 核心目标
 2) estimator_status_flags.cs_opt_flow        全程恒 0
 3) cs_ev_pos 在线占比 >99%，vision_data_stopped 0 次
 4) cs_rng_hgt 仅在 HAGL<5 m 时为 1                             (EKF2_RNG_CTRL=1)
 5) -z 与 sensor_baro.pressure 反算高度一致                     (EKF2_HGT_REF=0)
 6) eph/evh 分布明显变宽                                         (波次2 噪声参数生效)
 7) estimator_selector_status.primary_instance 切换 0 次        (EKF2_MULTI_IMU=1)
 8) 人为制造 ≥1 s EV 断流：cs_inertial_dead_reckoning 置位、xy_valid→0、
    执行 COM_POSCTL_NAVL 指定动作，且【速度状态不被任何脏数据改写】← 真正的验收试验
 9) ★新增★ estimator_status_flags.cs_ev_yaw 全程恒 0            (EKF2_EV_CTRL 11→3 生效)
10) ★新增★ heading_reset_counter 在人为制造 EKF 重置时不随 odom reset_counter 变化 ← 验证 §13.2 硬置路径已切断
11) ★新增★ EV 到飞控到达率 ≥20 Hz（读 ekf2_timestamps.visual_odometry_timestamp_rel）
12) ★新增★ estimator_aid_src_magnetic_heading / _vehicle_odometry_yaw 的
    innovation 与 reject_flags 均已落盘（依赖 SDLOG_PROFILE bit1）
13) ★新增★ 带外基准一致性：GNSS 双点定向地迹方位 与 vehicle_attitude.yaw 差 <10°
    ← 本文 §12.3 的 ~50° 只能这样判，创新量不可用作判据（§13.3）
```
